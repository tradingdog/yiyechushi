# 版本记录

## 当前版本

v0.78

## v0.78

1. 首页主菜互动模板已从“固定一双木筷夹起主菜”放宽为“按菜品结构自动选择最合适的餐具和互动动作”：汤、羹、煲类可用勺子或汤匙，牛排与整块肉可用刀叉，面、粉和长条食材可用筷子、叉子或夹子，餐具数量也不再固定为 1 件。
2. 首页 prompt 现在允许 AI 按菜品自行决定是一只手还是多只手参与互动，以及具体是舀汤、切开、夹起、叉起、捞起、翻面、分勺、蘸汁、卷起还是上桌整理等哪一种动作瞬间；不再把所有菜都压回同一种悬空夹菜模板。
3. 图解02到图解06与封面图的共享 prompt 也已同步补上这套“主菜出现时餐具 / 手数 / 动作自适应”的规则；若其它页面出现主菜成品、主菜局部或人与主菜互动镜头，也必须按菜品逻辑自行判断最合理的餐具组合和互动方式。

## v0.77

1. 放宽了图解02到图解06共用的 prompt 校验逻辑：延续页 prompt 现在不再因为最终文本里没显式写出完整菜名，或只是提到了“页面标题 / 内容卡1”这类结构词，就直接判成内容异常。
2. 图解页 prompt validator 现已改为“缺什么就补什么”的硬约束模式；若模型没有把固定菜名写进最终 prompt，会自动补回同一道菜锚点；若没有显式写出“不要出现程序化字段标签”，也会自动补回这条负面约束。
3. run_text_stage_with_validation_retry() 的重试日志与最终终止信息现在会带出最后一次具体异常原因，后续若仍发生内容异常，可以直接看到是缺结构、缺菜名、占位文本还是别的校验问题，不再只剩一句笼统提示。

## v0.76

1. 修复了文本阶段首轮偶发 TLS/SSL 读包异常未被识别为可重试的问题；request_text_generation() 与 request_multimodal_text_generation() 现在会沿异常链识别 ssl.SSLError、httpcore.NetworkError、httpcore.ProtocolError 等底层连接错误，并在同轮内自动重试。
2. is_timeout_error() 现在会沿异常链判断真实超时，而不再只看最外层异常对象；这样即使外层报错把底层 timeout 或 SSL 读异常包了一层，程序也不会误判成“不可重试”。
3. 当第一次请求只是代理、连接池或 TLS 握手阶段的瞬时抖动时，主流程会自动重建文本 client 并继续重试，不再需要手动再点第二次 main.py。

## v0.75

1. 图解01 首图的动作模板已放开：不再默认固定成“筷子夹起主菜后带滴汁/挂汁”的统一镜头；当前只要求保留筷子夹起主菜的动作感，角度、方向、停顿瞬间和构图距离都允许按菜品自由变化。
2. 首图主菜动作现在允许少量真实手部自然入镜，男女都可以，年龄感控制在 12 到 25 岁之间，并按菜品气质自由选择更贴切的年轻手部状态；仍禁止人物脸部和手部特写抢戏。
3. 主流程顺序已重排为：先生成图解01到图解06 -> 先筛前 6 组 publish 图 -> 用筛中的首图重写封面 prompt -> 再生成封面 -> 再单独筛封面；当 PUBLISH_AUTO_SELECT=1 且 PHOTOSHOP_AUTO_COMPOSITE=1 时，只对 publish 文件夹里的最终入选图片执行 Photoshop 合成，不再整轮处理所有候选图。

## v0.74

1. 取消图解01 首图上半部的“收藏提示”细条：现在创意菜谱文本结构、page01 prompt 模板和首图 validator 都不再把这条上方提示当成必填内容，避免与底部收藏关注横条重复。
2. 抖音图文描述模板已从固定安利腔改为更接近抖音菜谱用户的口语化写法：允许豆包更俏皮、更网络化地自由发挥，但显式禁用“这道原创融合的……”“先收藏，想做时……”“想吃时照着步骤做就稳……”这类机械模板开头和收尾。
3. 本地 publish fallback 文案也已同步去模板化，不再默认输出“想做的时候直接翻出来照着走”这一类固定句式，避免后续在非豆包路径下又回退成老口径。

## v0.73

1. 修复 publish 评审阶段对豆包评分结果解析过于脆弱的问题：tools/select_publish_images.py 现在兼容代码块包裹、尾逗号，以及 Python 风格字典的轻微格式漂移，避免评审在中途某一页因“Expecting value”直接中断整轮流程。
2. 将封面图默认尺寸从 2160x3840 收口为 864x1536，继续保持竖版 9:16，但像素规格改为与其它图接近的中等尺寸，不再把封面单独拉到过高分辨率。
3. image_generator.py 的 get_cover_image_settings() 默认值与当前 config.env 已同步改为 864x1536；当前主流程实际读取到的封面参数为 864x1536、medium、2 张。

## v0.72

1. 修复主流程图解01错用图解页图片配置的问题：image_generator.py 之前在真正生成图解01时误用了 get_tujie_image_settings()，导致即使 config.env 已把首图设为 OPENAI_IMAGE_QUALITY=high、OPENAI_IMAGE_COUNT=2，主流程仍会按图解页的 medium 和 3 张去跑。
2. 当前已改为图解01 单独读取 OPENAI_IMAGE_*，图解02到图解06继续读取 OPENAI_TUJIE_IMAGE_*，封面继续读取 OPENAI_COVER_IMAGE_*；这样首图、图解页、封面三套出图配置重新彻底分离。
3. 主流程启动图片阶段的日志也已改成分别打印“首图模型 / 图解模型 / 封面模型”，避免只看到“图解模型”就误以为图解01 也应该沿用图解页那套参数。

## v0.71

1. 封面 prompt 的收口方式继续加严到“自动补硬约束再校验”：当豆包返回的封面文本没有把 9:16 画幅、中轴竖排、背景避让或首图同场景字段说全时，image_generator.py 现在会把这些缺失要求自动补回最终 prompt，而不是直接判失败。
2. 这次当前真实输入“豆腐锅巴虾”里，v0.70 虽然已经放宽了标点和空格匹配，但豆包仍会把首图场景压缩成更短的概括句，例如只写“浅灰水磨石台面，浅米色亚麻餐垫，圆形白瓷盘铺脆锅巴摆酿馅炸虾，酸甜薄芡”；这种写法方向正确，但仍不包含完整的首图同场景锁定字段。v0.71 已改为把缺失的精确场景线直接补回封面 prompt。
3. 本地已用当前 dish_name.txt 的“豆腐锅巴虾”重新做 3 次真实封面回归，返回耗时分别为 9.6 秒、17.24 秒、26.83 秒，当前 3 次均通过封面校验，说明封面阶段这轮“连续 3 次内容异常”的主因已经进一步修掉。

## v0.70

1. 修复封面 prompt 过严误判：image_generator.py 里的通用 prompt 校验与封面场景锁定校验，现已改为忽略空格和标点后的等价匹配，不再要求豆包必须逐字逐标点输出完全同一串文本。
2. 这次真实失败样本“豆腐锅巴虾”里，豆包返回的封面 prompt 实际已经包含正确方向，但会写成“豆、腐、锅、巴、虾”竖排提示，或把首图场景字段改写成“器皿是米白色圆形骨瓷盘，盘底铺完整脆锅巴，酿虾沿盘边整齐摆放”这类等价表述；旧 validator 会把这种输出误判为“缺少菜名”或“缺少首图同场景锁定信息”，导致封面阶段连续 3 次失败。
3. 本地已对同一份“豆腐锅巴虾”封面 prompt 做 3 次真实豆包回归，返回耗时分别为 28.4 秒、20.64 秒、11.74 秒，当前 3 次均通过封面校验，说明这轮卡死的主因已经修掉。

## v0.69

1. image_generator.py 现已把文本请求的可重试异常从“只认 timed out 字符串”扩展到真实传输层异常：包括空消息 ReadTimeout、ConnectTimeout、ConnectError、ReadError、APIConnectionError 和 5xx 服务端波动等。此前这些偶发异常会直接打断主流程，重新运行一遍才恢复；现在会在同一轮流程里自动重试。
2. 文本请求在重试时会重建 OpenAI client，不再继续复用刚刚出过问题的那条连接；同时文本 client 的 timeout 已拆成显式的 connect/read/write/pool 四段，其中 read 继续使用当前文本超时，connect/write/pool 则收紧到较短等待，避免偶发网络抖动把主流程长时间卡在建连或池等待阶段。
3. 本地已用受控回归验证：此前不会被自动重试的 httpx.ReadTimeout('') 与 httpx.ConnectError('temporary failure') 现在都能在 request_text_generation() 中自动重试成功；真实豆包最小请求也已验证仍可正常返回 OK。

## v0.68

1. 修正 config.env 里豆包文本阶段实测耗时说明的注释口径：当前真实业务样本中的最慢返回值已记录到 45.14 秒，不再误写为 32 秒；超时配置继续保持 120 秒，不改逻辑，只纠正文档说明。

## v0.67

1. 对 doubao-seed-2-0-mini-260428 做了当前真实业务 prompt 压测：创意菜谱阶段 2 次实测返回耗时分别为 32.16 秒、22.86 秒；首图文生图 prompt 阶段 6 次实测返回耗时落在 11.81 到 45.14 秒之间。当前未复现“300 秒才返回”的情况，说明最近这轮主流程卡死的主因不是豆包响应过慢。
2. config.env 与 image_generator.py 已把文本超时从 300 秒收口到 120 秒；这个值相对本轮实测最慢 45.14 秒仍保留了约 2.6 倍余量，足够覆盖当前豆包文本请求，同时避免真实挂起时等待过久。
3. 修复首图文生图 prompt 的本地误判：validator 现已兼容豆包常见的等义居中表述与空白差异；若豆包遗漏了必须原样落版的首图卡片文案或居中硬约束，程序会把这些硬性文字补回最终 prompt 再校验。当前同一份“香煎虾饼”首图 prompt 连续 3 次真实豆包回归均已通过。
4. 在关闭自动造菜的真实文本链路下，本地已验证流程能够稳定穿过此前卡死的“文生图prompt”阶段，并继续进入图解02文案与图解02prompt 生成，说明此前用户看到的“文生图prompt连续 3 次内容异常”已不再是当前主阻塞点。

## v0.66

1. 修复 image_generator.py 中 request_text_generation() 成功请求后未返回结果的问题；此前豆包接口即使已经正常返回文本，本地封装也会把结果丢成 None，导致后续阶段把代码异常误判成“文本接口异常”或直接在下游中断。
2. 本地已用当前 config.env 的豆包配置完成最小真实请求验证：doubao-seed-2-0-mini-260428 可以成功返回 OK，说明当前机器到 Ark 接口的网络、DOUBAO_API_KEY 鉴权与账户可用性没有出现显性阻塞；本轮定位到的主因是代码返回链缺陷，不是豆包服务不可用。

## v0.65

1. image_generator.py 与 guide_generator.py 已把豆包/OpenAI 文本阶段统一改为 300 秒超时；同一个文本阶段若请求超时，会继续用当前同一模型重试，最多 3 次，超过后直接抛错终止，不再切备用文本模型。
2. 图解文案、图解 prompt、创意菜谱、首图 prompt、封面 prompt 与抖音发布文案这些文本阶段，已全部移除“超时或内容异常就切本地模板兜底”的旧逻辑；现在若模型返回空文本、结构不合规或关键内容缺失，会重新调用当前模型，最多 3 次，仍失败就终止本轮流程。
3. config.env 已把文本超时改为 300 秒，并新增 TEXT_REQUEST_RETRY_COUNT=3；本地已验证 get_text_request_timeout_seconds() 读到 300、get_text_request_retry_count() 读到 3，且 image_generator.py、guide_generator.py、main.py 的 py_compile 通过。

## v0.64

1. main.py 现已把抖音发布脚本正式接入主流程尾部：当前生成、Photoshop 合成与 publish 评审全部完成并打印结果后，如果本轮 output 目录下已存在 publish 文件夹，会继续调用 tools/douyin_publish.py 作为收尾步骤，把 publish 图集、标题、描述与封面自动接到抖音创作者页。
2. tools/douyin_publish.py 新增可复用的 publish_with_args() / publish_output_dir() 入口；这样既保留了原脚本命令行用法，也能被 main.py 直接复用，不再需要通过子进程绕一层命令行调用。
3. tools/douyin_publish.py 的描述输入框定位继续补强，兼容抖音编辑器在已有内容时移除 data-placeholder 的场景；当前已验证新的主流程接线可对现有 output 目录执行 dry-run 校验通过，main.py 与 tools/douyin_publish.py 的 py_compile 也已通过。

## v0.63

1. tools/douyin_publish.py 彻底修复抖音自定义封面仍回落到首图的问题：封面裁剪弹窗真实关闭后，脚本不再切回“选择封面”页签去点默认首图确认，而是先等待上传封面页出现“上下滑动图片选择封面区域 / 重新上传”等就绪信号，再直接点击该页底部“确定”返回主编辑区；若首击后主编辑区未恢复，会在同一页签内按原按钮重试，直到自定义 cover 真正落回封面卡片。
2. tools/douyin_publish.py 同时补强了描述输入框定位，兼容抖音编辑器在已有内容时移除 data-placeholder 的场景；现在重复运行脚本时，即使页面里已经残留旧描述，也能重新聚焦当前 contenteditable 编辑器并改写文本，不再因为“等待 描述输入框 超时”中断。
3. 本地已用真实抖音创作者页完整验证：脚本可从现有发布页继续运行，自动填充到待人工发布状态；最终封面卡片放大复核后已确认为 publish/cover.jpg，而不是首图 01.jpg。

## v0.62

1. tools/douyin_publish.py 修复了抖音自定义封面裁剪阶段的假成功问题：裁剪确认与弹窗检测现在只匹配当前可见的 modal，并在裁剪弹窗出现后先等待 1.5 秒再用 DOM click 点击“确定”，避免误点隐藏节点或在裁剪组件尚未就绪时过早点击，导致日志显示“已点击”但裁剪弹窗实际上并未关闭。
2. 由于裁剪弹窗现在会真实关闭，后续“上传封面确认 -> 切换选择封面 -> 应用封面”的链路终于能真正生效；本地已用真实抖音创作者页完整验证，主编辑页的封面卡片和右侧手机预览都已切成 publish/cover.jpg，同时脚本仍默认停在人工审核发布前。
3. .gitignore 新增 tools/chrome_manual_cdp/ 与 tools/.tmp_*.png，避免本地手工 CDP 资料目录和封面调试截图再次进入版本库候选集。

## v0.61

1. tools/douyin_publish.py 默认不再自动点击最终发布；脚本会自动处理到自主声明完成并停在待人工发布状态，只有显式传入 --auto-submit-publish 时才会继续自动提交。
2. tools/douyin_publish.py 读取已有 output 目录下的抖音图文描述文件时，现会按当前 config.env 里的 PUBLISH_REQUIRED_TOPICS 重新合并最终 5 个话题，并把修正后的标签回写到本地描述 txt，避免“先生成 output、后改 config”时继续沿用旧标签。
3. 修复抖音封面上传后的最后应用步骤：脚本在封面裁剪弹窗关闭后，会显式切回“选择封面”页签，再点击该阶段真正的确认按钮应用封面，不再卡在“上传封面”页签底部那个无效的确定按钮。本地已验证整条发布链路可顺利走到自主声明并停在人工发布前。

## v0.60

1. tools/douyin_publish.py 现已支持在检测不到 http://127.0.0.1:9222 时，自动拉起一个独立的 Chrome 自动化窗口，并使用 tools/chrome_automation_profile 作为独立资料目录；这样不再要求用户手动带 --remote-debugging-port 参数启动浏览器。
2. 脚本现已明确区分“普通已开 Chrome”和“独立自动化 Chrome”：普通 Chrome 仍不能被 Playwright 直接接管；脚本会自动启动可接管实例，但首次使用时需要用户在这个独立自动化 Chrome 里先登录一次抖音创作者中心。若未登录，脚本会快速报错并明确说明不是 publish 图片问题，也不是标签页没匹配到。
3. .gitignore 新增 tools/chrome_automation_profile/ 与 tools/douyin_publish_last_error.png，避免自动化浏览器资料目录和失败截图误入版本库。本地已验证：脚本可自动拉起独立 Chrome、拉起 9222 并锁定 creator.douyin.com 页面；当前若自动化 Chrome 未登录，会直接报“当前自动化 Chrome 打开的抖音创作者页还没有登录”。

## v0.59

1. tools/douyin_publish.py 现已把 Chrome CDP 连接失败翻译成明确中文提示；当 http://127.0.0.1:9222 没开或被拒绝时，脚本会直接说明“当前错误与 publish 图片无关”，并明确指出已经识别到的图文图片和封面数量，避免再把“待上传图片”误解成“没找到图片”。
2. tools/douyin_publish.py 同步把启动前日志从“待上传图片”改成“已识别上传图片”，并修正了 Playwright wait_for 的类型标注，当前已验证 py_compile 通过。
3. config.env 当前已把 PUBLISH_REQUIRED_TOPICS 设置为“十万个吃什么”，便于后续验证手动必带话题确实会进入最终 5 个抖音标签。

## v0.58

1. 新增 tools/douyin_publish.py，使用 Playwright 通过 Chrome 远程调试端口接管已打开的 creator.douyin.com 页面；脚本会默认读取 output\20260525_043309_葱香海参酿 目录下的抖音图文标题、抖音图文描述和 publish 图集，自动上传 01.jpg 到 06.jpg、单独上传 cover.jpg、逐字填写标题和描述，并完成“个人观点或臆测”自主声明选择。脚本支持 --dry-run 做本地预检，失败时会尝试保存 tools/douyin_publish_last_error.png 截图。
2. requirements.txt 新增 playwright 依赖；本地已验证 tools/douyin_publish.py 的 py_compile 与 --dry-run 通过。当前机器尚未开启 http://127.0.0.1:9222 的 Chrome 远程调试端口，因此本轮没有实际接管浏览器执行上传动作。
3. config.env 新增 PUBLISH_REQUIRED_TOPICS；image_generator.py 现已把这些手动话题视为最终 5 个标签里的必带项，会优先保留它们，再由程序自动补齐其余话题。自动补位的话题仍继续过滤菜名标签与 #阿叶造新菜。

## v0.57

1. tools/select_publish_images.py 已把图解01 首图与图解02到图解06的标题居中升级为 publish 评审硬门槛；单靠豆包口头判断不再算数，程序会先生成带中心参考线的辅助图交给豆包评审，再叠加本地像素偏移估算，对标题中心相对画面中轴的偏移做双重判定。
2. 若某个首图或图解页当前所有候选图都未通过标题居中硬门槛，正式模式会自动读取该页已经落盘的文生图 prompt，按当前 config.env 中的模型、尺寸和质量参数单张补生新图，再把新图并回候选重新评审；dry-run 只会把“需要补生复评”的结论写进报告，不会真正调图片接口。
3. 当待评审目录里的图片已经是 Photoshop 合成后的 JPG 时，补生出来的新 PNG 会自动只对这一张补跑一次 PSD 模板，再参与复评，避免出现“旧图是合成 JPG、新图是原始 PNG”混评的问题。为此 tools/apply_photoshop_template_batch.py 新增了单张图片处理 helper。
4. 若标题补生阶段遇到图片接口阻塞，例如当前机器返回 gpt-image-2 的 403 unsupported_country_region_territory，选图工具不再整轮报错退出；该页会被明确标记为“未入 publish”，阻塞原因会写进 publish_selection_report.json 和 publish_selection_report.txt，其余已通过的页面仍会继续进入 publish。
5. 主流程现已正式接入自动 publish 收尾：image_generator.py 会在整套图片生成完并完成 Photoshop 合成后，根据 PUBLISH_AUTO_SELECT 自动调用 tools/select_publish_images.py，在 output/日期时间戳_菜品名 下创建 publish，并把评分报告路径与入选图片路径回传给 main.py 打印。
6. config.env 新增 PUBLISH_AUTO_SELECT 与 PUBLISH_TITLE_CENTER_RETRY_LIMIT 两个公开配置；requirements.txt 新增 Pillow，用于生成标题居中评审辅助图。

## v0.56

1. tools/select_publish_images.py 修复了 dry-run 不创建 publish、也不写评分报告的问题；现在无论 dry-run 还是正式运行，都会在目标菜品目录下创建 publish，并落盘 publish_selection_report.json 与 publish_selection_report.txt。

## v0.55

1. 新增 tools/select_publish_images.py，可按页面自动分组某个 output/日期时间戳_菜品名 目录中的多版本图片；图解01、图解02到图解06和封面图会分别按不同维度投喂给豆包评审，并把每页最高分图片移入或复制到新建的 publish 文件夹，同时输出 publish_selection_report.json 和 publish_selection_report.txt 两份评分报告。
2. 图片评审工具现已把“品牌污染”约束收紧为只检查实体物体上的品牌、logo、包装、瓶身、锅具印字和食材标签；页面自带的菜名、标题、页尾关注文案与账号名“阿叶造新菜”不再被误判为硬伤。若某页只有 1 张图，工具会直接入选，不调用 AI 分析。
3. 新增 run_photoshop_template_batch.cmd，支持在 Windows 下双击列出 output 子目录并调用本地 Photoshop 批处理工具；也支持直接把某个 output 子目录作为参数传入，方便独立补跑 PSD 模板合成。
4. 修复图解01 page01 prompt 在文本阶段回退时会错误地从粗略 notes 猜食材、步骤和成败关键的问题；image_generator.py 现已优先从已经生成好的正式菜谱文本反解析 bundle，并把食材卡、成败关键、5 步和底部文案逐项带回 page01 prompt，避免再把主料退成菜名或把步骤退成通用模板。
5. guide_pages/page01_recipe.py 与 image_generator.py 已同步收紧首图 prompt 约束和校验：现在不仅校验中轴与居中，还会核对引导句、副标题、食材卡、成败关键、步骤卡和底部文案是否真正进入最终 prompt；若缺任一关键文案，就会切回基于正式菜谱文本重建的本地 prompt。旧的“豆腐 / 耙豌豆”专用负面词也已改为按当前菜动态注入，不再污染无关菜。
6. Git 流程规则已补强：项目规范现明确要求任何代码、脚本、配置或文档改动，在任务结束前都必须完成版本更新、本地提交和推送 GitHub；若远端阻塞，则必须在项目记忆中记录原因，不允许再把大量版本长期滞留在本地工作区。

## v0.54

1. 主流程现已正式接入本地 Photoshop 后处理；generate_recipe_assets_from_idea_file() 会在全部 gpt-image-2 图片落盘后，自动对本轮 output/日期时间戳_菜品名 目录执行 PSD 模板合成，并把 png/jpg 统一覆盖导出为 jpg。
2. config.env 已删除 Adobe Firefly Services、S3 中转和头像相关配置，只保留本地 Photoshop 所需的 PHOTOSHOP_AUTO_COMPOSITE、PHOTOSHOP_LOCAL_EXE、PHOTOSHOP_TEMPLATE_FILE、PHOTOSHOP_TEMPLATE_SMART_OBJECT_LAYER、PHOTOSHOP_JPEG_QUALITY、PHOTOSHOP_JOB_TIMEOUT_SECONDS；requirements.txt 里的 boto3 也已移除，touxiang_generator.py 已删除。
3. 为避免用户先花完生图额度再在最后一步失败，主流程在真正生图前会先校验本地 Photoshop、PSD 模板和 input_image 智能对象层；若 Photoshop 正在运行，也会在开跑前直接拦下。
4. 抖音图文标题现已强制规范化为“菜名，卖点！”；即使模型返回缺少中文逗号、中文叹号或把菜名和卖点粘在一起，落盘前也会统一修正成固定格式。
5. 抖音图文描述最后 5 个话题现已过滤掉菜名话题与 #阿叶造新菜，并回落到更稳的高频美食搜索词/活动型话题池。之所以没有接入伪实时抓取，是因为抖音网页和巨量算数公开页都存在签名或 JS 渲染拦截，不适合直接挂进主流程。
6. 本地已验证：重构后的 tools/apply_photoshop_template_batch.py 在新配置名下 dry-run 通过；标题归一化、话题过滤和 Photoshop 主流程预检 helper 也已通过直接调用验证。

## v0.53

1. tools/apply_photoshop_template_batch.py 新增本地 Photoshop 模式；传入 --local-photoshop-exe 指向 Photoshop.exe 后，脚本会直接在本机调用 Photoshop 执行 JSX，打开 PSD 模板、定位命名智能对象层、替换图片并导出高质量 JPG 覆盖原图。
2. 本地模式不再依赖 Adobe Firefly Services 和 S3，因此即使没有 PS_FF_SERVICES_CLIENT_ID、PS_FF_SERVICES_CLIENT_SECRET、ADOBE_PS_S3_BUCKET、ADOBE_PS_S3_REGION 这些云配置，也能完成模板批处理。
3. 本地链路已按实际环境验证过三种执行方式：Photoshop.exe 的 -r 参数不可用，COM/VBScript 桥接在当前机器上也未注册成功；最终确认可行方案是直接把 JSX 文件路径传给 Photoshop.exe，由 Photoshop 本体执行脚本。
4. 为了避免误关用户已经打开的 Photoshop 工作区，本地模式现在会先检查 Photoshop 是否已经在运行；若已运行，会要求先手动关闭后再执行本地批处理。
5. 本地已验证：使用 D:\Program Files\Photoshop\App\Program Files\Adobe\Adobe Photoshop 2026\Photoshop.exe 和 tools/photoshop_template/template.psd，对 output\20260525_045739_酸香煎鲈鱼 成功完成 dry-run 与正式批处理，13 张 png 图片均已被模板重混并覆盖为 jpg。

## v0.52

1. 修复图解01 首图副标题会在非豆腐菜上误写成“豆腐两面煎焦香”的问题；根因是 image_generator.py 里的本地卖点推断把“豆腐 / 金黄 / 焦色 / 煎”放进同一组任意命中条件，导致只要菜名或说明里出现“煎”就会触发豆腐卖点。
2. 同时收紧了黄豌豆、咸香与挂汁相关卖点的推断逻辑；现在豆腐卖点必须真的命中豆腐类食材，黄豌豆卖点必须真的命中黄豌豆/耙豌豆，非豆腐煎制菜会改成通用的煎香卖点，不再回落到固定豆腐文案。
3. 修复图解01 本地兜底“成败关键”也会在非豆腐菜上误写出“豆腐先煎出焦边再合炒”的同类污染问题；现在相关提示同样要求先命中真实豆腐或黄豌豆食材，普通煎制菜会改成通用煎香提示。
4. 创意菜谱校验链已新增副标题纠偏：若文本阶段返回的副标题仍混入不属于当前菜本体的“豆腐两面煎焦香”或“黄豌豆压到软糯”，程序会基于“菜名 + 主画面食材 + 主料 + 汤汁/质感”等可信字段在落盘前自动改写。
5. 本地已验证：煎小牛膝排这类非豆腐菜的本地副标题现会输出“煎香一口就记住 挂汁到位更入味”这类通用描述，成败关键不再出现豆腐提示；错误副标题也能被 validate_recipe_text_content() 自动纠正。

## v0.51

1. 新增 tools/apply_photoshop_template_batch.py，提供一个独立于主流程的 Photoshop 云 API 批处理工具；它会把指定目录中的 .jpg/.jpeg/.png 逐张替换进 PSD 模板的命名智能对象层，再导出为高质量平面 JPG 覆盖原图。
2. 首版工具采用“本地文件 -> S3 中转 -> Photoshop 云 API -> 平面 JPG 回写本地”的链路设计；脚本会自动上传 PSD 模板和输入图片到 S3、生成预签名 URL、调用 documentManifest 校验模板图层、调用 smartObject 接口替换图片，再调用 renditionCreate 导出 JPG。
3. config.env 已新增 Photoshop 工具专用配置，包括 Adobe Firefly Services 的 Client ID / Client Secret / 可选 access token，以及 S3 桶、区域、临时前缀、URL 过期时间、任务超时、模板路径、智能对象层名、JPG 质量和是否清理远端中间文件。
4. requirements.txt 已新增 boto3 依赖，用于 S3 上传、下载和预签名 URL；tools/photoshop_template/README.md 也已补充模板准备说明，明确默认 PSD 路径是 tools/photoshop_template/template.psd，默认智能对象层名是 input_image。
5. 本地已验证：新脚本 py_compile 通过、错误检查通过、命令行 --help 可正常输出；并补充了两处收口修正，支持“手动 access token 模式下不强制 client secret”，同时限制输入范围为 .jpg/.jpeg/.png，避免误把 PSD 当成待覆盖源图。

## v0.50

1. 修复封面图默认尺寸 1080x1920 无法用于 gpt-image-2 的问题；根因不是 9:16 比例本身，而是 1080 不能被 16 整除。
2. 根据 OpenAI 官方当前文档，gpt-image-2 的尺寸约束为：最长边不超过 3840，宽高都必须是 16 的倍数，总像素介于 655360 和 8294400 之间，长短边比例不超过 3:1。项目已将封面默认尺寸改为合法满规格 9:16 的 2160x3840。
3. image_generator.py 新增图片尺寸本地校验；主图、图解页和封面图在读取尺寸配置时都会先验证格式、16 倍数、最长边、比例和总像素，避免非法尺寸直到请求 API 时才返回 400。
4. config.env 与 image_generator.py 的封面默认尺寸已同步改为 2160x3840。本地已验证：get_cover_image_settings() 现在返回 2160x3840，旧的 1080x1920 会被本地校验以“宽和高都必须是 16 的倍数”直接拦下。

## v0.49

1. 修复图解01 首图上方标题区仍可能整体偏左的问题；首图标题区不再只写“水平居中”，而是改为明确要求“沿同一条画面正中竖线向下堆叠的中心柱布局”。
2. guide_pages/page01_recipe.py 已补入首图专用中轴文案，明确要求顶部引导句、主标题、黄条卖点和收藏提示条的中心点都落在同一条画面正中竖线上，并显式禁止从左边起排、主标题偏左后再用黄条补右、收藏提示条单独缩到左边。
3. image_generator.py 新增 validate_page01_prompt_content() 首图专用校验；若最终首图 prompt 缺少“中轴 / 中心点 / 中心柱布局 / 左右留白对称 / 禁止偏左”这类硬约束，会直接判异常并切回本地首图模板，不再让偏松的首图 prompt 继续落盘和出图。
4. 本地已验证：旧版“葱香海参酿”首图 prompt 会被新校验以“缺少首图标题中心柱布局约束”拦下；新的本地首图模板可以通过校验，且 image_generator.py、guide_pages/page01_recipe.py 的 py_compile 与错误检查均通过。

## v0.48

1. 将封面图比例从竖版 2:3 调整为竖版 9:16，封面默认尺寸改为 1080x1920，更贴近短视频封面比例。
2. guide_pages/cover_page.py 已同步改写封面模板：除了把画幅口径改成 9:16，还额外强调这是“更高更窄”的长竖版构图，上半段给竖排菜名留出更长通道，主菜和餐盘更多压到中下段，避免沿用旧版 2:3 的铺满式构图。
3. image_generator.py 的封面尺寸配置默认值和封面 prompt 校验已同步切到 9:16；若封面 prompt 仍缺少 9:16 画幅约束，会直接判异常并切回本地封面模板。
4. 本地已验证：封面本地模板已不再残留 2:3，且明确包含 9:16 与“中下段”构图要求；封面尺寸默认值现已切到 1080x1920。

## v0.47

1. 调整主流程输出结构；程序现在会在 output 下按“日期时间戳_菜品名”创建单次运行目录，并把本轮所有文本文件、prompt 文件、图片文件、revised prompt 和抖音发布文案全部放进同一个目录。
2. image_generator.py 新增单次输出目录 helper，并把创意菜谱、图解文案、图解 prompt、封面 prompt、抖音图文标题、抖音图文描述、最终图片和 revised prompt 的保存路径统一收口，不再继续拆到 output/chuangyi、output/prompt、output/image、output/publish 四个子目录。
3. 单独调试 prompt 生图链路也已同步调整；generate_images_from_prompt_file() 现在会把原始 prompt 与生成图片一起保存到同一个 output/日期时间戳_菜品名 目录。
4. 本地已验证：打桩触发本地兜底后，整套文本与 prompt 结果只会落到一个 output/20260525_152016_鲜笋酿肉卷 目录；单独调试生图链路的原始 prompt 文件与图片文件也会落到同一个单次目录。

## v0.46

1. 修复独立封面图里菜名竖排容易偏到左侧的问题；封面 prompt 现在不再只写“垂直居中”，而是强制要求画布正中保留整条竖向标题通道。
2. guide_pages/cover_page.py 已把封面标题约束收紧为“单列竖排压在中轴线上”，并显式禁止靠左贴边、靠右偏移、双列排布、蛇形排布和上下错位；同时要求背景主菜、餐盘与陪衬主动退到下半部、右下或左下，避免挤占中轴标题位。
3. image_generator.py 新增封面 prompt 中轴校验；若最终 prompt 没写出“中轴 / 竖向标题通道 / 单列竖排”以及“背景避让中轴”的硬约束，会直接判异常并切回本地封面模板。
4. 本地已验证：旧版“香煎蒜苔肥牛卷”封面 prompt 会被新校验以“缺少菜名单列竖排中轴约束”拦下；新的本地封面模板可以通过校验。

## v0.45

1. 修复首图不同菜仍会收敛到相似“石面 / 水磨石 + 小碟陪衬”拍摄环境的问题。
2. image_generator.py 现已新增对“模板化场景字段”的识别；当豆包产出的“桌面与环境”“背景陪衬”过于泛化，例如落回浅灰石面、水磨石、简洁干净、小碟失焦等常见模板时，归一化阶段会自动切换到本地更贴菜的场景推断结果。
3. 本地场景推断已补入更细的首图分支，单独区分“酸汤/木姜子类煎鱼片摆盘”和“黑椒/春卷皮类脆煎里脊卷”场景，避免再把炸卷误判成蒸卷，也避免煎鱼和炸卷继续挤在同一类石面环境里。
4. 创意菜谱 system prompt 和 user prompt 已同步收紧：不同出餐结构的菜必须主动拉开桌面材质、器皿逻辑和后景陪衬，不能只在同一套石面模板里换黑白盘。
5. 本地已验证：旧版“酸香煎鲈鱼”首图文案会从“浅灰色水磨石台面，简洁干净”自动改为更亮的浅米石面场景；“脆煎里脊卷”会改为深色出菜台/耐热垫场景，不再继续落回浅灰水磨石模板。

## v0.44

1. 修复封面背景图容易回落到“暖色陶盘 + 木桌 + 暖光虚化”固定模板的问题，封面背景现已强制沿用首图同一道菜的器皿、桌面、陪衬、主菜主体和酱汁场景。
2. guide_pages/cover_page.py 现会把首图 bundle 中的“器皿与摆盘 / 桌面与环境 / 背景陪衬 / 主画面食材 / 酱汁或汤汁状态 / 质感重点 / 色彩点缀”整套场景字段写进封面 prompt，而不是只给一个泛化的暖色虚化背景。
3. image_generator.py 新增封面 prompt 专用场景锁定校验；若文本模型返回的封面 prompt 没有明确带上首图同场景信息，会直接判异常并切换到本地封面模板兜底。
4. 本地已验证：旧版“葱香海参酿”封面 prompt 会被新校验拦下；新的封面本地模板已明确包含白色长方盘、深色石面、一小碟葱段、透明玻璃壶和棕亮酱汁等首图同场景信息。

## v0.43

1. 修复多图解图片 prompt 会混入“当前菜名”“页面标题”“页面副标题”“页尾提示”等程序化字段标签的问题，避免这些后台说明词被直接画进图片。
2. guide_pages/shared.py 现已把多图解文案转换成“只包含实际展示文字值”的摘要后再交给图片 prompt 阶段，不再把【页面信息】、内容卡编号、当前页面等结构标签原样喂给下一阶段。
3. image_generator.py 新增多图解专用 prompt 校验；若最终 prompt 仍混入上述程序化字段标签，会直接判为异常并切回本地干净兜底，不再带病进入生图阶段。
4. 本地已验证：旧版图解04 prompt 会被新校验拦下；新的图解04 本地兜底 prompt 已不再包含任何“当前菜名 / 页面标题 / 页面副标题 / 页尾提示”类程序化字段标签。

## v0.42

1. 修复创意菜谱阶段“引导句”和“副标题”会复用同一组词的问题；现在会把上下两行卖点近似重复视为异常并自动重写顶部引导句。
2. image_generator.py 新增营销文案相似度校验，能识别“完全相同”“只差空格”以及“只多或少 1 到 2 个字”的近似重复情况，避免把黄条卖点直接搬到菜名上方。
3. 创意菜谱 system prompt 和 user prompt 已同步收紧：明确要求顶部引导句负责钩子点，主标题下方副标题负责 2 到 3 个卖点词，两者不能上下换行后重复使用同一组词。
4. 本地已验证：当引导句是“软嫩葱香层次绝”、副标题是“软嫩 葱香 层次”时，程序会自动把顶部引导句改回“这盘端上就抢光”这类钩子句，而保留下方卖点词。

## v0.41

1. 修复创意菜谱归一化阶段会无条件覆盖豆包产出的“器皿与摆盘”“桌面与环境”“背景陪衬”字段的问题；现在只在字段缺失或明显占位时才启用本地补齐。
2. 收紧 image_generator.py 的首图场景本地推断逻辑，新增只提取摆盘、装盘、上桌、桌面、台面等相关语句的场景上下文，不再被“淀粉”“白胡椒粉”“蒸锅”“脆甜”这类步骤或口感泛词误判。
3. 新增蒸卷、腐皮、酿类热菜的器皿与桌面分支，避免这类菜再次落回炸物盘、面碗石面或固定白色水磨石模板。
4. 本地已验证：鲜笋鸡丝蒸酿腐皮当前会推断为浅口圆盘加亚麻/木台热菜场景，且若豆包已给出自定义器皿、桌面和背景字段，归一化后会原样保留。

## v0.40

1. 修复图解01 首图主菜照片长期收敛成“木桌 + 暖陶盘 + 木托 + 小碗陪衬”同一套模板的问题。
2. image_generator.py 现已把主画面说明扩展为“器皿与摆盘 / 桌面与环境 / 背景陪衬 / 主画面食材 / 汤汁或酱体 / 质感重点 / 色彩点缀”多维字段，并在归一化阶段按菜名和做法自动推断这些信息。
3. 首图的本地推断逻辑现会按菜本身的上桌逻辑区分砂锅、深口汤碗、长鱼盘、铸铁盘、搪瓷盘、石面、水磨石、亚麻餐垫等更贴近菜品的器皿和桌面，不再默认回到木桌陶盘。
4. guide_pages/page01_recipe.py 与 临时调试prompt.txt 已同步收紧：统一海报 VI 仍然保留，但餐具、桌面与后景小物必须服从菜品本身，不能每道菜都复用同一套背景模板。
5. 本地已用文本链路验证：首图 prompt 现已能为“橄榄腌煎羊菲力”生成铸铁盘 + 石面/水磨石 + 亚麻餐垫 + 蒜片/青柠的更贴合场景，而不是继续落到旧的木桌陶盘模板。

## v0.39

1. 新增自动造菜前置流程；默认开启后，主程序会先从 chuantongcaipu.txt 按菜系范围随机抽一条传统菜做参考，让豆包自动生成新的菜名与长描述，并回写到 dish_name.txt，再继续后续 prompt 和生图链路。
2. config.env 新增 AUTO_GENERATE_DISH_IDEA、AUTO_DISH_CUISINE_MODE、AUTO_DISH_LIBRARY_FILE、AUTO_DISH_MEMORY_FILE 四个配置项；其中菜系范围支持 0 全部随机、1 中餐、2 新马泰/东南亚、3 日韩、4 西餐/欧洲、5 中东北非、6 东欧、7 拉美。
3. 新增 dish_idea_memory.jsonl 记忆文件机制，自动记录已用传统参考菜和已生成新菜名，程序会据此规避重复参考菜和重复新菜名。
4. 新增 tools/generate_auto_dish_idea.py，可单独只刷新 dish_name.txt，而不继续生成图解 prompt 或图片。
5. 本地已验证：tools/generate_text_prompts_from_dish_name.py 能在自动造菜开启时先改写 dish_name.txt 再走完整文本链路；tools/generate_auto_dish_idea.py 也能独立运行并写入记忆文件。

## v0.38

1. 重排 chuantongcaipu.txt，改成更适合程序读取的“中餐 / 国外”双层分类结构；每个大区下继续保留 15 个主烹饪方式分类。
2. 在国外部分继续新增 196 道中东、北非、东欧、拉美传统菜名，重点补足黎巴嫩、土耳其、伊朗、埃及、摩洛哥、突尼斯、俄罗斯、波兰、匈牙利、捷克、罗马尼亚、格鲁吉亚、墨西哥、秘鲁、巴西、阿根廷、智利、古巴、哥伦比亚、委内瑞拉等方向。
3. 新增条目覆盖巴巴加努什、法图什沙拉、雪克舒卡、哈里拉汤、曼萨夫、库纳法、波兰酸黑麦汤、罗马尼亚牛肚汤、墨西哥鼹酱鸡、巴西海鲜炖锅、阿根廷玉米炖肉等高频传统菜。
4. 本地已校验 chuantongcaipu.txt 当前共有 939 道菜名，其中中餐 560 道、国外 379 道，且无重复项。

## v0.37

1. 修复图解01 首图顶部引导句过度固定的问题，移除创意菜谱 system prompt 中对“周末请客就做这锅”这类老套标题范式的强引导。
2. 创意菜谱生成阶段现已明确要求引导句使用更高变化的抖音美食短钩子语感，允许按场景、情绪、结果、反差和口感自由发挥，不再默认落回“周末请客”“家宴”“请客就做”“有面子”这类陈词滥调。
3. image_generator.py 中的 infer_guide_line() 已改为按菜名和补充说明动态生成更活的兜底短句，不再只返回“今天就做这盘”“请客就做这盘”“就馋这口热乎”这类固定句。
4. validate_recipe_text_content() 现会自动检查 AI 产出的引导句；若命中“周末请客”“家宴”等老词、长度不合规或直接重复菜名，会自动替换成动态兜底钩子句。
5. 本地已验证：新的引导句生成与自动纠偏逻辑不会再落回“周末请客”“家宴”这类固定老词。

## v0.36

1. 继续扩充 chuantongcaipu.txt，在现有按主烹饪方式分类的结构下新增 183 道国外传统菜名，总量由 560 道提升到 743 道。
2. 新增范围重点覆盖泰国、新加坡、马来西亚、印尼、越南、菲律宾、日韩、印度、俄罗斯、法国、意大利、西班牙、英国、德国、土耳其、中东和拉美等常见传统菜系。
3. 已补入用户点名缺失的冬阴功汤、罗宋汤，并同步补充海南鸡饭、肉骨茶、叻沙、西班牙海鲜饭、玛格丽特披萨等高认知度经典国外菜。
4. 新增菜名继续沿用现有 15 个烹饪方式分类，不另起新结构，便于人读和后续程序按类读取。
5. 本地已校验 chuantongcaipu.txt 当前共有 743 条菜名，且无重复项。

## v0.35

1. 收紧图解01 首图模板中的标题排版约束，新增“引导句、主标题、黄条卖点、收藏提示条都必须严格以画布中轴线水平居中”的硬要求，减少主标题左右漂移。
2. 收紧图解02到图解06共享模板中的标题排版约束，页面标题和副标题现在都要求严格水平居中，页码可单独放左上，但不能再把标题区挤偏。
3. 图解01 现已明确要求主菜成品图必须是一整张主体背景大图，作为标题下方到步骤条上缘的统一照片底层存在，不允许再生成带矩形边框、白边或圆角相框的独立照片卡。
4. page01_recipe.py 和 shared.py 的 system prompt、user prompt 与本地兜底 prompt 已同步补上这些约束，避免只在单一链路生效。
5. 本地已验证新的首图与多图解 prompt 文本都包含“严格居中”约束，且 page01_recipe.py、shared.py 的 py_compile 校验通过。

## v0.34

1. 修复文本阶段异常返回仍会被直接写入 output/chuangyi 和 output/prompt 的问题。
2. image_generator.py 和 guide_generator.py 现已为创意菜谱、多图解文案、主图 prompt、封面 prompt 增加最小结构校验；遇到空文本、占位文本或明显缺字段结果时，会自动切到本地模板兜底，不再落盘垃圾文件。
3. save_text_output() 现在会拒绝保存空文本，避免再出现真正的空文件。
4. 本地已验证：故意返回“图解05文案 输出”“封面prompt 输出”这类占位内容时，程序会切本地兜底，且不会把占位文本写进输出文件。
5. 当前已清理 20260524_004106、20260524_004138、20260524_004209、20260524_004244 这几组异常输出文件。
6. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.34。

## v0.33

1. 主流程在文本模块生成完创意菜谱、主图 prompt、多图解 prompt 和封面 prompt 后，新增一段抖音发布文案生成流程。
2. 新增的发布文案包括两部分：抖音图文标题和抖音图文描述；描述最后会自动带 5 个更贴合当前菜谱内容的潜力话题标签。
3. 新文案统一保存到 output/publish，文件名分别以“抖音图文标题”和“抖音图文描述”结尾。
4. tools/generate_text_prompts_from_dish_name.py 现在也会同步生成抖音发布文案；tools/generate_images_from_prompt_folder.py 新增 --text-only 模式，可只根据 tools/prompt 下的 txt 生成抖音发布文案而不生图。
5. 本地已验证主流程文本阶段会产出新的发布文案文件，且 prompt-folder 工具的 --text-only 模式不会调用图片客户端。
6. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.33。

## v0.32

1. 修复封面模板仍会带出顶部引导句、卖点小字、收藏条和关注文案的问题。
2. cover_page.py 现在把封面规则收紧为“只允许菜名文字 + 背景菜图”，并显式要求除菜名外画面其它区域 0 文字。
3. 封面 prompt 生成时不再注入整份菜谱文案，也不再把参考一页菜谱 prompt 原文整段塞进 system prompt，避免把额外文字模块再次带回封面。
4. 本地已验证封面阶段实际 user prompt 只保留菜名、纵向排版和背景菜品描述，不再混入整份菜谱字段。
5. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.32。

## v0.31

1. 把主流程中“先生成创意 txt 和文生图 txt，再开始生图”的前半段抽成 generate_recipe_text_assets_from_idea_file()，作为新的公共文本入口。
2. 新增 tools/generate_text_prompts_from_dish_name.py，固定读取 dish_name.txt，只生成创意 txt、图解文案 txt、各页文生图 txt 和封面文生图 txt。
3. 该新工具不会调用 gpt-image-2，也不会生成任何图片，输出目录仍沿用 output/chuangyi 和 output/prompt。
4. 本地已用打桩方式验证：新的文本-only 流程即使把图片客户端设为报错，也能正常完成全部 txt 输出，说明不会误触发图片生成。
5. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.31。

## v0.30

1. 新增 tools/generate_images_from_prompt_folder.py，用于脱离主流程单独生图。
2. 该脚本固定扫描 tools/prompt 目录，每个 txt 文件都作为一条完整图片 prompt，按 config.env 当前的 OPENAI_IMAGE_* 参数逐个调用 GPT Image 2 生图。
3. 若 tools/prompt 目录下没有 txt 文件，脚本会直接结束，不生成任何图片，也不会报“缺文本后仍误生图”。
4. 本地已验证空目录时脚本会直接退出；也已用假生图函数验证多 txt 文件会按顺序逐个处理。
5. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.30。

## v0.29

1. 重新核对 OpenAI 官方 GPT Image 2 文档、成本表和定价表后，确认当前官方默认别名仍是 gpt-image-2，最新公开快照是 gpt-image-2-2026-04-21。
2. 项目继续锁定 gpt-image-2-2026-04-21 作为默认图片模型，同时把主图、图解页、封面图与头像的默认图片质量统一改为 medium，以提升性价比。
3. config.env 注释已补充 image_count 会同时提高成本并按图片张数占用 IPM，方便按层级限额与预算自行调整。
4. 本地已验证当前活动值为 gpt-image-2-2026-04-21，且主图、图解页、封面图默认质量都已切换为 medium。
5. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.29。

## v0.28

1. 已确认 OpenAI 官方当前公开的 GPT Image 2 最新快照为 gpt-image-2-2026-04-21，并已将项目默认图片模型切换到该版本。
2. 新增 config.env 作为统一运行参数文件，图片模型、质量、数量、尺寸、超时、文本模型等参数都可在这里直接修改。
3. config.env 支持每个参数后面直接写同一行中文注释，程序会自动忽略注释部分并正确读取前面的值。
4. 运行时配置优先级已改为外部环境变量 > .env > config.env；其中 .env.example 收口为密钥示例，避免继续和实际默认参数打架。
5. 本地已验证当前实际生效值为 gpt-image-2-2026-04-21，封面尺寸为 1024x1536，说明新配置链路已经生效。
6. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.28。

## v0.27

1. 封面图模板已从竖版 3:4 改为竖版 2:3，封面默认图片尺寸也同步改为 1024x1536。
2. 用于 AI 生成的首页、多图解页和封面模板里的账号昵称已统一从“一页厨”改为“阿叶造新菜”。
3. 首页底部关注文案已替换为“关注@阿叶造新菜，家用开店都不赖！”，并通过 guanggaoyu.txt 动态注入。
4. 图解02到图解06的图片 prompt 现在也会统一带上一条与首图风格一致的细长底部关注文案，文字固定为“关注@阿叶造新菜，家用开店都不赖！”，且明确不生成图标。
5. 本地已验证封面模板只剩 2:3、多图解 prompt 已带新广告语，相关脚本 py_compile 通过。
6. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.27。

## v0.26

1. 图解01 首图模板新增固定尾句，最终 prompt 的最后一行会原样追加“请优先保证主菜图片看起来像真实手机实拍，饱和度降低20%，再完成整张海报的文字和排版。”。
2. 首图的系统提示和用户提示都已同步约束模型把这句话放到最终输出 prompt 的末尾，避免被模型提前吸收或改写。
3. 本地 prompt 模板也已同步把这句话放在最后一行，确保兜底输出与正式链路一致。
4. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.26。

## v0.25

1. 图解01 首图模板已按最新实拍测试版 prompt 的方向单独强化，主菜大图明确要求像先用 iPhone 主摄 1x 默认相机直出拍好的真实晚饭照片，再排进整张海报里。
2. 首图新增了更具体的拍摄条件：非人像模式、30 到 45 度轻微俯拍、中近景构图、整盘大部分区域清晰、只有轻微自然虚化、晚饭室内暖光加侧面自然光、不要奶油景深和电影感虚化。
3. 首图继续保留“筷子夹起主菜悬空”的动作感，但明确要求是生活化抓拍，不是电商主图式夸张英雄展示。
4. 首图背景道具已收紧为普通木桌、少量失焦餐具和少量必要食材点缀，避免因为堆砌香料和装饰配件把主图推向假广告质感。
5. 本地已验证 page01_recipe.py 编译通过，且构造出的首图 prompt 同时包含更强的 iPhone 直出实拍约束和悬空夹菜动作约束。
6. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.25。

## v0.24

1. 图解01、图解02到图解06和封面图的图像内容约束已统一收口为 iPhone 主摄 1x 默认相机直出照片感。
2. 现在只要 prompt 里出现任何菜品、食材、半成品、局部状态、汤汁、配料或背景食物，都会被统一要求写成真实手机直出照片，而不是商业棚拍或电商主图。
3. 同时继续保留原有爆款海报 VI，不让真实拍摄约束外溢成整张普通纪实照片。
4. 本地已验证主图、图解页、封面 3 个入口构造出的 prompt 都包含 iPhone 主摄直出、不要商业棚拍补光、不要电商主图感等关键约束。
5. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.24。

## v0.23

1. 主流程再次修正为先完成全部文案和 prompt，再统一进入图片生图阶段。
2. 已用本地打桩验证顺序，确认第一张图片的生图调用发生在全部文本与 prompt 完成之后。
3. 文本端继续保持豆包 1.8/2.0 系列独立客户端，图片端继续使用 gpt-image-2。
4. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.23。

## v0.22

1. 豆包文本模型默认 ID 已更新为 doubao-seed-2-0-pro-260215。
2. 文本调用链继续保持与生图调用链分离，生图端仍然使用 gpt-image-2，不受影响。
3. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.22。

## v0.21

1. 主流程运行顺序已调整为先完成全部创意文案和 prompt 保存，再统一调用 gpt-image-2 生成所有图片。
2. 图解01、图解02到图解06以及封面图现在会在同一阶段完成 prompt 产出后，再进入统一生图阶段。
3. 维持文本端豆包、图片端 gpt-image-2 的双客户端结构不变，仅改变执行顺序。
4. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.21。

## v0.20

1. 文本调用链与生图调用链已分离：文本请求改为独立文本客户端，生图请求继续使用独立图片客户端。
2. 文本侧新增豆包配置支持，默认文本提供方为 doubao，默认文本模型为 doubao-seed-1-8-251228，支持通过 TEXT_API_PROVIDER 在 doubao/openai 间切换。
3. 生图模型配置保持不变，图解与封面仍默认使用 gpt-image-2 系列配置，不随文本模型切换。
4. 本地已验证 get_text_provider()=doubao、get_text_model()=doubao-seed-1-8-251228、get_image_settings()['model']=gpt-image-2，且 py_compile 与污染回归脚本通过。

## v0.19

1. 新增回归检查脚本 tools/check_prompt_pollution.py，用于检测图解02到图解06本地兜底文案中的固定菜词污染。
2. 脚本同步检测图解02到图解04的 PAGE_DEFINITION.visual_focus 是否出现“豆腐/肉沫/黄豌豆”固定词污染。
3. 脚本内置 py_compile 校验，自动检查 image_generator.py、guide_generator.py 与 guide_pages/*.py 语法可用性。
4. 脚本已补充仓库根目录导入路径，支持直接执行 python tools/check_prompt_pollution.py，不需要手动设置 PYTHONPATH。
5. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.19，并补充脚本运行说明。

## v0.18

1. 修复图解02到图解04的 PAGE_DEFINITION.visual_focus 固定写死“豆腐、肉沫、黄豌豆”问题，改为通用菜品视觉描述。
2. 清理图解04本地兜底文案中残留的“先炒肉沫再煎豆腐”固定句，改为动态通用工序表述。
3. 复验图解02到图解06本地兜底文案与图解02到图解04视觉焦点后，不再命中固定词“豆腐/肉沫/黄豌豆”。
4. 本地已完成 python -m py_compile image_generator.py guide_generator.py guide_pages/*.py 校验通过。

## v0.17

1. 修复图解02到图解06本地兜底文案写死“豆腐肉沫”语义的问题，改为按当前 dish_name.txt 对应的 bundle 动态生成。
2. 图解02、图解03、图解04、图解05 的本地兜底文案已改为优先读取当前主料与菜名变量，不再绑定固定菜名场景。
3. 图解06 补充对当前菜名变量的动态引用，确保不同输入菜名下尾页文案也能随之变化。
4. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.17。

## v0.16

1. 修复“真实拍摄约束把整张海报 VI 一起带偏”的问题，恢复首图、多图解页和封面图原有的标题、排版、边框、配色与整体爆款海报节奏。
2. 恢复图解01 原本的筷子夹起主菜英雄镜头，只在主菜食材和局部照片层保留真实拍摄约束，不再把整张图改成普通纪实照片。
3. 多图解页与封面图改为只优化卡片图片或背景菜品的真实质感，不再改变整页 VI、字体、边框和整体色调。
4. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.16。

## v0.15

1. 只调整 7 张图片 prompt 模板中的拍摄与质感指令，统一把首图、多图解页和封面图往真实手机或相机实拍方向收紧。
2. 图解01 主图不再强调悬空夹起整块主菜的广告镜头，改为允许筷子在画面边缘自然入镜，保留更生活化的动作感。
3. 图解01、多图解页与封面图补充了自然偏暖、低饱和、低对比、非棚拍假光、食材形态随机不整齐等真实拍摄约束。
4. README.md、项目记忆.md、项目规范.md 已同步更新到 v0.15，避免文档仍保留旧的悬空夹菜描述。

## v0.14

1. 将图解01一页菜谱与封面图也拆分到 guide_pages 独立脚本中，当前形成“每张图片一个脚本”的页面结构。
2. 新增 guide_pages/page01_recipe.py 与 guide_pages/cover_page.py，分别作为图解01和封面的页面逻辑来源。
3. image_generator.py 改为以通用素材整理、本地推断、OpenAI 调用和流程编排为主，不再内置主图与封面的长 prompt 模板。
4. 修复页面拆分清理过程中引入的 image_generator.py helper 区断裂与缩进错误，恢复本地 fallback 所需的 infer_* 函数。
5. 本地已完成主流程相关模块 py_compile 校验，并完成图解01、图解02到图解06、封面图的无 API 本地构造验证。

## v0.13

1. 主流程从“主图加封面”扩展为“6 张图解加 1 张封面”，其中图解01保留为当前一页菜谱核心主图。
2. 图解02到图解06 的固定阅读顺序调整为：食材怎么挑 -> 关键细节拆解 -> 省时技巧与工具 -> 平替方案 -> 口味调整。
3. 新增 guide_generator.py，负责统一编排后 5 张多图解的文案、prompt 与出图流程。
4. 新增 guide_pages 目录，并将图解02到图解06拆分到独立页面脚本，避免所有页面逻辑堆在一个文件里。
5. 主流程现在会把图解01到图解06的文案保存到 output/chuangyi，对应 prompt 保存到 output/prompt，最终图片保存到 output/image。
6. 新增 OPENAI_TUJIE_IMAGE_MODEL、OPENAI_TUJIE_IMAGE_SIZE、OPENAI_TUJIE_IMAGE_QUALITY、OPENAI_TUJIE_IMAGE_COUNT 配置入口，默认 1 张图解图。
7. 本地已完成多图解分页模块与主流程的 py_compile 编译校验，未调用线上 API 做实测生图。

## v0.12

1. 新增封面图分支，在菜谱图生成完成后继续生成独立封面 prompt 与封面图。
2. 封面图固定为竖版 3:4，默认尺寸为 1056x1408，默认输出 1 张。
3. 封面图文件名会在菜名后追加“封面”，并与菜谱图一起落到 output/image，封面 prompt 同步落到 output/prompt。
4. 封面 prompt 强制沿用当前一页菜谱 VI，要求底部使用带光圈虚化效果的菜品背景，菜名按红色描边主标题样式纵向居中排布。
5. 当封面 prompt 文本接口超时时，主流程会改用本地封面模板兜底，保持整条链路不断。
6. main.py 已补充封面 prompt 文件与封面图输出信息。

## v0.11

## v0.11

1. 强制主标题菜名直接使用 dish_name.txt 第一行原文，不再允许模型擅自改名。
2. 收紧引导句和黄条卖点的长度约束，标题上下卖点改为更短更适合海报阅读的节奏。
3. 主图新增“一双筷子夹起一块主菜悬空”的动作镜头约束，提升食欲感。
4. 收藏提示文案改为“先收藏，想做时直接照着买照着做”。
5. 补充文本模型备用模型与文本/图片请求超时配置。
6. 当文本接口超时时，主流程会自动切换备用模型，仍失败则使用本地模板兜底继续生成 prompt 与图片。
7. 实测当前 dish_name.txt 输入下，新的菜谱文本、文生图 prompt 与抽检图片均已生成。

## v0.10

1. main.py 切换为读取 dish_name.txt 的完整生成流程入口。
2. image_generator.py 新增“创意菜谱生成 -> 文生图 prompt 生成 -> 最终生图”三段式流程。
3. 新增 output/chuangyi、output/prompt、output/image 三类输出目录规则。
4. 新增 OPENAI_TEXT_MODEL 配置示例，并补充 output 忽略规则。
5. 同步更新 README.md、项目规范.md、项目记忆.md。

## v0.09

1. 新增 touxiang_generator.py，内置 10 个一页厨头像 prompt。
2. 头像输出目录改为 touxiang，并补充对应忽略规则。
3. 补充头像相关环境变量示例。
4. 同步更新 README.md、项目规范.md、项目记忆.md。

## v0.08

1. 将截图1模板中的配方与步骤统一改为 g、ml、L 等物理量单位。
2. 将高汤统一改为清水，降低新手执行门槛。
3. 调整成败关键短句，去除不必要的逗号。
4. 调整底部 CTA 文案，突出新口味蹄花认知。
5. 将默认出图数量改为 2 张，便于比选。

## v0.07

1. 同步最新广告语变量内容，更新 guanggaoyu.txt 为“一页一道新菜”。
2. 同步更新 README.md、项目规范.md、项目记忆.md。

## v0.06

1. 继续优化截图1模板下的标题层级，强化“引导句小于菜名”的爆款标题结构。
2. 收紧主菜蹄花块提示，压制对称复制、圆柱假肉块和过平切面。
3. 同步更新 README.md、项目规范.md、项目记忆.md。

## v0.05

1. 将临时调试 prompt 切换为截图1风格的爆款海报模板。
2. 保留 guanggaoyu.txt 广告语变量注入。
3. 放宽主菜蹄花表现形式，改为截图1风格的大块蹄花成品图。
4. 同步更新 README.md、项目规范.md、项目记忆.md。

## v0.04

1. 将底部关注文案改为从 guanggaoyu.txt 动态注入。
2. 收紧临时调试 prompt，对蹄花数量、重量和真实结构做更严格约束。
3. 新增保存本次实际渲染 prompt 的调试输出。
4. 同步更新 README.md、项目规范.md、项目记忆.md。

## v0.03

1. 完成 GitHub CLI 登录。
2. 创建 GitHub 远程仓库 tradingdog/yiyechushi。
3. 推送本地 main 分支并建立 origin/main 跟踪关系。
4. 同步更新 README.md、项目记忆.md、版本记录.md 的仓库状态。

## v0.02

1. 补充 main.py 和 image_generator.py 的中文进度输出与异常提示。
2. 实测运行 main.py 成功生成图片到 image 目录。
3. 初始化本地 git 仓库并完成首个中文版本提交。
4. 尝试创建 GitHub 远程仓库，但因 gh 未登录而暂时受阻。

## v0.01

1. 初始化主程序入口 main.py。
2. 初始化图片生成模块 image_generator.py。
3. 新增临时调试 prompt 文件 临时调试prompt.txt。
4. 新增中文项目规范与项目记忆文档。
5. 补充 .env.example 和 .gitignore。
