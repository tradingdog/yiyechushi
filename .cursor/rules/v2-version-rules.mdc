---
description: V2 版本号与文档同步规则；有代码/配置/文档改动时自动遵循
alwaysApply: true
---

# V2 版本规则

适用于本仓库 `V2/` 及关联脚本、配置。完整规范见 `V2/项目规范.md`，当前版本号以该文件「当前版本」为准。

## 何时算一次版本变更

只要 `V2/` 内发生**新增、修改、删除**（代码、脚本、配置、文档），即视为一次版本变更。

## 版本号

- 从 **v0.01** 起，每次变更 **+0.01**（如 v0.73 → v0.74）。
- 面板 UI 改动时同步更新 `V2/web_panel.py` 中的 `PANEL_VERSION`。
- 同步更新 `V2/项目规范.md` 的「当前版本」。

## 每次版本变更必须更新的文档

1. `V2/版本记录.md` — 新增该版本条目（日期 + 变更摘要）
2. `V2/项目记忆.md` — 记录根因、决策或后续注意点（如有）

## 任务结束前的顺序

1. 先完成版本号与上述文档更新
2. 再 `git status` 检查
3. 再本地提交与推送 GitHub（提交标题：`v版本号：中文说明`）

## Git 与 GitHub（与版本变更配套）

- 提交标题格式：`v版本号：中文说明`
- 默认流程：更新版本号 → 更新文档 → `git status` → 本地提交 → 推送 GitHub
- 从 v0.11 起，V2 任务默认由 AI 自动完成上述 git 流程，不要求用户手动 Review/Commit
- 本地存在未提交改动时，不得直接结束任务；须纳入当前版本并提交推送，或记录阻塞原因

## 例外

- 用户**当次明确要求**不提交、不推送时，不执行 git 操作，但在 `V2/项目记忆.md` 记录原因（优先于默认自动提交流程）。
- 远端客观阻塞时：先完成本地提交，在 `V2/项目记忆.md` 记录阻塞原因。

## 语言

说明文档、版本记录、项目记忆、提交说明以**中文**为主；技术字段/库名/接口名可保留英文。
