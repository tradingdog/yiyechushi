# 项目记忆

## 当前版本

v0.08

## 当前阶段

第一阶段：搭建最小可运行的图片生成程序骨架。

## 当前目录结构要点

1. main.py 负责启动生成流程。
2. image_generator.py 负责读取 .env、加载 prompt、调用 gpt-image-2、保存图片。
3. 临时调试prompt.txt 存放当前调试使用的完整 prompt 模板。
4. guanggaoyu.txt 负责提供关注文案变量。
5. image 目录由程序运行时自动创建并保存图片。

## 已确认事项

1. OpenAI 图片模型默认使用 gpt-image-2。
2. 默认图片尺寸为 1024x1536，对应竖版 2:3。
3. 默认图片质量为 high。
4. 文件名需带日期和时间戳。
5. 已实测运行 main.py，并生成首张图片到 image 目录。
6. 本地 git 仓库已初始化，首个提交已完成。
7. GitHub 远程仓库已创建，仓库地址为 https://github.com/tradingdog/yiyechushi 。
8. 本地 main 分支已关联 origin/main。
9. 程序已支持将 guanggaoyu.txt 注入 prompt，并保存实际渲染后的 prompt 文本。
10. 当前主图模板方向已切换为截图1风格，不再强逼整只蹄花特写。
11. 当前调优重点为：标题引导句和菜名的大小层级分离，以及蹄花块避免对称复制和圆柱假肉块。
12. 当前广告语变量内容为：关注@一页厨，一页一道新菜，家用开店都不赖。
13. v0.08 起默认每次生成 2 张图。
14. v0.08 起配方与步骤统一改用 g、ml、L 等物理量单位，并将高汤改为清水。

## 当前阻塞与待办

1. 继续调优截图1模板下的主菜真实性和标题转化文案。
2. 后续每次目录有改动，都需要同步更新版本、提交并推送远程仓库。
