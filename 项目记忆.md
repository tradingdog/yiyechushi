# 项目记忆

## 当前版本

v0.90

## 当前阶段

第一阶段：完成从菜品创意到 6 张图解图片加独立封面图的完整生成链路。

## 当前目录结构要点

1. main.py 负责启动生成流程。
2. image_generator.py 负责读取 dish_name.txt、整理通用素材 bundle、调用页面脚本，并串联总流程。
3. guide_generator.py 负责生成图解02到图解06的文案、prompt 与出图结果。
4. guide_pages 目录下的独立脚本分别定义图解01到图解06及封面图各自的内容目标、版式方向和本地兜底文案。
5. dish_name.txt 第一行是用户菜名或创意，第二行起是补充说明。
6. 临时调试prompt.txt 存放当前截图1风格的固定 VI 参考模板。
7. guanggaoyu.txt 负责提供关注文案变量。
8. config.env 负责提供可公开调整的运行参数，并支持每行尾部中文注释。
9. output 目录下会按“日期时间戳_菜品名”创建单次输出文件夹。
10. 每次生成的图解文案、图解 prompt、封面 prompt、最终图片、revised prompt、抖音图文标题与抖音图文描述，都会统一放进对应的单次输出文件夹里，不再拆分子目录。
11. tools/generate_text_prompts_from_dish_name.py 可单独读取 dish_name.txt，只生成当前菜对应单次输出文件夹内的文本结果，不调用图片模型。
12. tools/apply_photoshop_template_batch.py 已收口为本地 Photoshop 批处理工具；main.py 在 PHOTOSHOP_AUTO_COMPOSITE=1 时会在全部图片落盘后自动调用它。
13. tools/photoshop_template/template.psd 是默认 PSD 模板路径；默认承接输入图的智能对象层名为 input_image。
14. PHOTOSHOP_LOCAL_EXE 必须指向 Photoshop.exe；主流程开启自动合成时，会先做 Photoshop 与模板预检，再继续生图。
15. run_photoshop_template_batch.cmd 可在 Windows 下双击列出 output 子目录并调用本地 Photoshop 批处理工具，也支持把目标子目录直接作为参数传入。
16. tools/select_publish_images.py 会按页面分组 output 子目录里的多版本图片，使用豆包多模态按首图 / 图解页 / 封面三套维度分别打分，并把每页最佳图移动或复制到 publish 文件夹，同时输出 JSON 和 TXT 评分报告。
17. 图解01 首图与图解02到图解06的标题居中现在是 publish 评审硬门槛；当前首图只信豆包判断，本地像素偏移只保留在报告里做辅助备注；图解页仍保留 AI 判断与本地估算双兜底。若当前候选全部未通过，正式模式会按该页原 prompt 单张补生后复评。
18. 当待评审目录已经是 Photoshop 合成后的 JPG 时，标题补生出来的新图会自动只对这一张补跑一次 PSD 模板，再回到候选集评审；若补生阶段被 gpt-image-2 的 403 unsupported_country_region_territory 挡住，该页不会入 publish，但阻塞原因会写进报告，其余页面继续处理。
19. 主流程在 PUBLISH_AUTO_SELECT=1 时，会在整套图片生成与 Photoshop 合成完成后自动创建 publish、执行发布图筛选并输出评分报告。
20. 图解01 首图的本地兜底卖点与成败关键现在都必须按真实食材命中；不能再因为“煎 / 焦香 / 软糯 / 香”这类泛词，把非豆腐菜误写成豆腐或黄豌豆提示。
21. 抖音图文标题现在会强制规范化为“菜名，卖点！”；描述最后 5 个话题会过滤掉菜名话题与 #阿叶造新菜。
22. tools/douyin_publish.py 可接管已开启远程调试的 Chrome 抖音创作者页；默认读取某个 output 子目录下的抖音图文标题 txt、抖音图文描述 txt 和 publish 目录，自动上传 01.jpg 到 06.jpg、cover.jpg，并完成自主声明选择。
23. config.env 新增 PUBLISH_REQUIRED_TOPICS；一旦配置，抖音图文描述最终 5 个话题里必须优先保留这些手动话题，剩余名额才由程序自动补齐。
24. 若 tools/douyin_publish.py 运行时报“无法连接到 Chrome 远程调试端口”，那不是图片缺失；脚本在连接浏览器前就已经完成 publish 图片、封面、标题和描述文件识别，当前机器需要先把 Chrome 用 --remote-debugging-port=9222 方式启动起来。
25. v0.60 起：tools/douyin_publish.py 在 9222 不可用时会自动拉起独立的 Chrome 自动化窗口，默认复用 tools/chrome_automation_profile 目录；这避免了手动带调试参数启动浏览器，但自动化窗口不会继承当前普通 Chrome 的登录态。
26. v0.60 起：脚本已补充登录页快速识别；如果自动化 Chrome 还没登录抖音创作者中心，会直接报错提示“请先在这个自动化 Chrome 窗口里完成一次抖音登录”，不再长期卡在首页发布按钮定位。
27. v0.61 起：tools/douyin_publish.py 默认只自动处理到待人工发布状态，不再自动点击最终发布；若要恢复自动提交，需显式传 --auto-submit-publish。
28. v0.61 起：tools/douyin_publish.py 读取已有 output 下的抖音描述文件时，会按当前 config.env 的 PUBLISH_REQUIRED_TOPICS 重新合并 5 个标签并回写 txt，避免生成后再改 config 时仍沿用旧话题。
29. v0.63 起：抖音封面流程已最终收口为“裁剪弹窗真实关闭 -> 等上传封面页出现 cover 预览就绪态 -> 直接点上传页底部确定回主编辑区”；不能再切回“选择封面”页签点默认确认，否则会把封面覆盖回首图。当前上传页底部确定有时需要在预览真正出现后再点击，必要时同页签可重试一次到两次。
30. v0.63 起：抖音图文描述编辑器在已有内容时会移除 data-placeholder；tools/douyin_publish.py 的描述输入框定位不能只依赖“添加作品描述”占位属性，需兼容当前真实的 contenteditable 编辑器节点，否则重复运行脚本会在描述输入阶段超时。
31. v0.64 起：main.py 已把 tools/douyin_publish.py 接到主流程最后一步之后；若本轮 output 目录下已有 publish 文件夹，主流程会在打印完本轮结果后继续执行抖音发布收尾。douyin_publish 模块也已补出可复用入口，主流程不再需要借助子进程拼命令调用。
32. v0.65 起：文本生成不再允许任何本地模板兜底；当前规则是豆包/OpenAI 文本链路单个文本阶段超时会继续用当前同一模型最多重试 3 次；若连续超时或连续内容校验失败超过 3 次，就直接终止整轮流程。
33. v0.66 起：若文本链路刚改完重试逻辑后又出现“接口异常”或下游 NoneType，需要先核查 request_text_generation() 是否真的把成功结果 return 出去；2026-05-26 已实测当前 DOUBAO_API_KEY + Ark 接口可成功完成最小请求，故当日出现的持续异常主因不是余额或网络，而是本地封装漏返回。
34. v0.67 起：豆包 doubao-seed-2-0-mini-260428 在当前真实业务样本里的文本返回时间实测主要落在 12 到 45 秒；当前把文本超时收口到 120 秒已足够，不必再盲目上调。若首图 prompt 继续报“内容异常”，优先排查 validator 是否把等义居中表述、空格差异或豆包对卡片文案的轻微改写误判为失败。
35. v0.69 起：主流程里偶发的文本阶段卡死或首个请求直接异常，不能只靠 is_timeout_error() 里的字符串匹配判断；httpx.ReadTimeout('')、ConnectTimeout、ConnectError、ReadError 这类瞬时传输异常也必须自动重试。文本请求重试时需要重建 client，避免继续复用已经出过问题的连接。
36. v0.70 起：封面 prompt 比首图 prompt 更容易把菜名写成拆字竖排提示，也更容易把首图场景字段改成只差逗号或停顿词的等价表达；封面 validator 不能再按原样整串命中去卡，否则会出现“封面prompt连续 3 次内容异常”，但真实返回其实方向正确。
37. v0.71 起：仅靠“忽略标点和空格后的等价匹配”仍不够；封面 prompt 经常会把首图场景压缩成概括句，导致同场景锁定字段不全。当前需要像首图 prompt 一样，在 validator 里把缺失的 9:16、中轴竖排、背景避让和首图同场景硬约束补回最终 prompt 再校验。
38. v0.72 起：图解01 一页菜谱的真实出图必须走 get_image_settings()，不能误用 get_tujie_image_settings()；否则 config.env 里的首图 OPENAI_IMAGE_QUALITY 和 OPENAI_IMAGE_COUNT 会被图解页的 OPENAI_TUJIE_IMAGE_* 覆盖，表现成首图错误地跑成 medium + 3 张。
39. v0.73 起：封面图默认尺寸已从 2160x3840 收口到 864x1536；继续保持竖版 9:16，但像素规格与其它图接近，不再把封面单独拉到满规格。若封面构图突然明显失衡、主体过大或标题通道被挤压，先核对 OPENAI_COVER_IMAGE_SIZE 是否又被调回超高分辨率。
40. v0.74 起：图解01 首图上半部已取消“收藏提示”细条；当前顶部只保留引导句、主标题和黄条卖点。若后续首图又出现“先收藏，想做时……”这类上方短条，优先检查 page01 prompt 模板、本地 bundle 文本结构和首图 validator 是否把这条字段重新加回。
41. v0.74 起：抖音图文描述模板不能再统一写成“这道原创融合的……”或“先收藏，想做时……”。当前应优先让豆包按抖音菜谱账号口吻自由发挥，更口语、更俏皮、更像真人在发图文，但仍保留实用信息和最后 5 个话题标签约束。
42. v0.75 起：图解01 首图动作模板已从“筷子夹起主菜后默认带滴汁/挂汁”收口为更开放的动作要求；现在只要求有筷子夹起主菜即可，角度、方向、停顿瞬间和构图距离都允许按菜品自由变化，并允许少量 12 到 25 岁的年轻手部自然入镜。若首图又回到统一滴汁镜头，优先检查 DEFAULT_DYNAMIC_ACTION 和 page01 的 chopsticks requirement 是否被改回旧模板。
43. v0.75 起：当 PUBLISH_AUTO_SELECT=1 时，主流程顺序已改为“先生成图解01到图解06 -> 先筛前 6 组 publish -> 用筛中的首图重写封面 prompt -> 再生成封面 -> 再单独筛封面 -> 最后只处理 publish 目录里的入选图”。因此 Photoshop 不再默认处理整轮所有候选图，封面 prompt 也不再只靠文字描述，而会吸收已筛中的首图作为视觉参考。
44. v0.76 起：若主流程第一次刚进入自动造菜就偶发报错，而第二次手动重跑又恢复正常，优先怀疑文本接口首轮 TLS/SSL 读包异常没有被正确判成可重试。当前 request_text_generation() 与 request_multimodal_text_generation() 已改为沿异常链识别 ssl.SSLError、httpcore.NetworkError、httpcore.ProtocolError 和 timeout；这类首轮瞬时异常应在同轮自动重建 client 并重试，不该再要求人工重跑。
45. v0.77 起：图解02到图解06共用的 validate_guide_page_prompt_content() 已放宽，不再因为最终 prompt 没显式写出完整菜名，或 prompt 说明里提到了“页面标题 / 内容卡1”这类结构词，就直接判成内容异常。当前逻辑会先做非占位校验，再把缺失的菜名锚点和“不要出现程序化字段标签”负约束补到最终 prompt 里。若延续页 prompt 仍连续失败，优先看 run_text_stage_with_validation_retry() 打出来的最后一次具体异常原因。
46. v0.78 起：首页与其它出现主菜的页面，餐具和互动动作都已改为按菜品自适应，不再默认固定成一双木筷。当前 prompt 会允许模型按汤/羹/煲、牛排/整块肉、面/粉/长条食材、铁板/锅物/共享菜等不同结构，自行选择勺子、汤匙、刀叉、筷子、夹子、锅勺、分餐勺等组合，并决定是一只手还是多只手、以及舀汤、切开、叉起、捞起、翻面、分勺、蘸汁、卷起等哪种互动瞬间。若后续画面又回到固定木筷模板，优先检查 build_adaptive_food_interaction_requirement() 和 page01 的主画面互动要求是否被改回旧口径。
47. v0.79 起：首图底部步骤区已从固定【5步出锅】放宽为【3步出锅】、【4步出锅】或【5步出锅】三种动态结构。当前文本模板、步骤解析和首图 prompt 都会按实际步数渲染，越少越好，但不能少于 3 步，也不能超过 5 步；若首图又回到固定 5 步，优先检查 build_recipe_step_section_title()、extract_recipe_step_section_title()、validate_recipe_text_content() 和 page01 的步骤条文案是否被改回旧口径。
48. v0.79 起：图解06“口味调整”的视觉引导已不再默认固定成旋钮或滑杆。当前 page06_taste_adjustment.py 应允许用刻度条、分段条、层级块、色带、波形、路标、温度计、堆叠标签等不同数值表现形式来表达口味变化；若图解06 又开始每次都生成同一套旋钮仪表盘，优先检查 PAGE_DEFINITION 的 layout_requirements、visual_focus 和 negative_constraints。
49. v0.84 起：publish 标题居中补生图会继续写回原 output 目录，方便人工直接看到新增候选；因此图解01 候选张数可能从初始 2 张逐步增加到 3 张、4 张、5 张，这是当前设计，不是额外重复生图的异常。
50. v0.80 起：tools/select_publish_images.py 里 page01 的补生参数已改为读取 OPENAI_IMAGE_*，不再误用 OPENAI_TUJIE_IMAGE_*；若后续再出现“首图补生质量/尺寸不对”，优先检查 resolve_regeneration_image_settings()。
51. v0.81 起：仓库根目录新增 run_douyin_chrome_debug.bat，可双击拉起带 9222 调试端口的独立 Chrome；它固定复用 tools/chrome_automation_profile，和 tools/douyin_publish.py 自动拉起浏览器时用的是同一套登录态与资料目录。
52. v0.81 起：run_douyin_chrome_debug.bat 提供 --check 预检模式，可先确认本机 Chrome 路径是否被找到、资料目录是否就绪，以及 http://127.0.0.1:9222 当前是否已经可访问，再决定是否真正启动浏览器。
53. v0.82 起：main.py、image_generator.py 与 tools 目录下所有可直接执行的 Python 脚本，都会在根目录 logs 自动新建一份独立日志；日志文件名形如“时间戳_脚本名_pid.log”，同时保留控制台输出和完整报错。
54. v0.82 起：run_douyin_chrome_debug.bat 也会把自身输出写入根目录 logs；若用户反馈“脚本没留痕”，先检查 logs 是否已有对应时间戳的新文件，而不是先去 output 目录找。
55. v0.83 起：自动造菜会在生成前读取最近 10 条历史新菜名，并提取其中已经用过的主食材、口味和主烹饪方式作为禁用项；这组禁用项会同时进 prompt 和本地 validator，避免连续几轮都重复“鱼 / 豆腐 / 煎”这类方向。
56. v0.83 起：自动造菜的近历史禁用规则目前主要依赖菜名词表抽取；若后续仍出现明显重复，优先扩充 AUTO_DISH_MAIN_INGREDIENT_FAMILIES、AUTO_DISH_FLAVOR_FAMILIES 和 AUTO_DISH_PRIMARY_METHOD_FAMILIES，而不是先放松 validator。
57. v0.85 起：首图标题居中判定已收口为只信豆包；当前 candidate_title_centering_pass() 在 page01 上不会再让本地像素偏移参与通过与否。若首图仍出现误判，优先检查豆包返回的 title_centering_pass、title_center_offset_percent 与评审 prompt，而不是先调本地阈值。
58. v0.84 起：补生图重新写回原 output 文件夹，不再放进临时目录后清掉；若用户说“没看到补生图”，优先检查 select_publish_images.py 的 regenerate_group_candidate() 是否还在往 input_dir 写入，而不是去找 _publish_regen_cache。
59. v0.86 起：normalize_recipe_text() 不得再把“动态动作”强行覆盖回 DEFAULT_DYNAMIC_ACTION；若豆包已给出具体动作就必须保留，只有空值、占位文本或“自行安排餐具”这类模板话时才回退到 infer_dynamic_action_description()。
60. v0.86 起：图解01 对酿、夹心、内馅、包裹类主菜必须出现露芯画面；当前要求是一块主菜被人真实咬开或掰开，清楚露出内部食材和汁水。若首图没露出内部结构，优先检查 page01_recipe.py 的 filled requirement 和 bundle['dynamic_action'] 是否进了最终 prompt。
61. v0.86 起：图解01 的器皿与摆盘不允许再写成整齐复制模板；当前程序会自动补入人手自然码放、轻微错位、高低差、疏密变化和少量盘边汁痕这类语义。若摆盘又回到机械整齐，优先检查 normalize_plate_description()、build_page01_plating_naturalness_requirement() 和首图 prompt 校验补丁。
62. v0.86 起：封面 prompt 已收口为更简洁的同场景封面口径；validator 不再要求 prompt 逐条复写器皿、桌面、背景、主食材、酱汁、质感、色彩全部字段。若封面标题又被挤到上半部，优先检查 build_cover_scene_lock_description() 和 validate_cover_prompt_content() 是否被改回长清单。
63. v0.87 起：封面 prompt 改为固定模板句式，cover_page.py 的系统提示、用户提示和本地兜底都优先沿用一条简洁模板，不再让模型扩写复杂条件列表；若封面 prompt 又变长，优先检查 build_cover_fixed_prompt_template()、build_cover_prompt_system_prompt() 和 build_cover_prompt_user_prompt()。
64. v0.87 起：封面比例从 9:16 调整回 2:3，默认尺寸为 1024x1536；image_generator.py 中 get_cover_image_settings()、append_cover_hard_requirements()、validate_cover_prompt_content() 与 build_cover_reference_prompt_user_content() 必须保持同一口径。
65. v0.88 起：图文标题文件命名从“_抖音图文标题.txt”改为“_图文标题.txt”；tools/douyin_publish.py 需优先读新文件名，找不到时再兼容旧文件名。
66. v0.88 起：图文描述已拆成多平台版本，当前会产出抖音（5）、小红书（10）、微信视频号和公众号（30）、快手（4）的“平台话题.txt”和“平台图文描述.txt”；描述正文模板共用，只替换不同平台话题。
67. v0.88 起：多平台话题生成阶段给豆包的关联文档固定为 dish_name.txt，不能混用其它文档；同时继续过滤 #阿叶造新菜 与菜名本身话题，并在本地做数量兜底。
68. v0.89 起：config.env 已新增三平台必带话题配置：PUBLISH_REQUIRED_TOPICS_XIAOHONGSHU、PUBLISH_REQUIRED_TOPICS_WECHAT、PUBLISH_REQUIRED_TOPICS_KUAISHOU，默认留空；程序会像抖音一样按平台分别读取并优先保留对应平台必带话题。
69. v0.90 起：tools/douyin_publish.py 已兼容封面关键词命名，不再只认 cover.*；publish 目录中凡是文件名包含“封面”或“cover”的图片都可作为封面识别。脚本会先锁定封面再排除出图文上传列表，避免误把封面当普通图文图上传。
70. v0.90 起：已在 output/20260528_203406_鲜辣鱿鱼猪蹄煲 上实测通过自动发布流程，封面文件“...封面_01.jpg”可被正确识别并上传，流程停在待人工发布状态，未自动点击最终发布。

## 已确认事项

1. OpenAI 图片模型当前默认使用 gpt-image-2-2026-04-21。
2. 文本生成默认使用 OPENAI_TEXT_MODEL，.env.example 当前示例值为 gpt-4.1-mini。
3. v0.67 起：文本链路不再切备用文本模型，也不再落本地模板；当前统一使用 120 秒超时和 3 次重试，超过后直接终止流程。
4. 图解页默认尺寸为 1024x1536，对应竖版 2:3。
5. 默认图片质量为 medium。
6. 图解页默认图片数量为 1。
7. 封面图默认尺寸为 1024x1536，对应竖版 2:3。
8. 封面图默认图片数量为 1。
9. 文件名需带日期和时间戳。
10. main.py 当前会按“图解01一页菜谱 -> 图解02到图解06 -> 封面图”顺序执行。
11. v0.52 起：validate_recipe_text_content() 已接入首图副标题纠偏；若副标题混入不属于当前菜本体的“豆腐两面煎焦香”或“黄豌豆压到软糯”，会基于菜名、主画面食材、主料、汤汁与质感字段自动改写后再落盘。
12. v0.53 起：本地 Photoshop 模式已在当前机器实测跑通；可用的调用方式是直接把 JSX 文件路径传给 Photoshop.exe，-r 参数不可用，COM/VBScript 桥接在当前机器上也未注册成功。
13. v0.54 起：主流程已正式接入本地 Photoshop 自动合成；PHOTOSHOP_AUTO_COMPOSITE=1 时会在真正生图前先校验 Photoshop 与模板，全部图片落盘后再统一覆盖导出 JPG。
14. v0.55 起：page01 与后续图解/封面 fallback bundle 不能再直接从 idea notes 粗猜，必须优先从已经生成好的正式菜谱文本反解析字段；否则 prompt 阶段一旦回退，就会把主料退成菜名、把香料/调味料和步骤退成通用模板。
15. v0.55 起：page01 的 prompt 校验不只检查中轴和居中约束，还会校验引导句、副标题、食材卡、成败关键、步骤卡和底部文案是否真实进入 prompt；缺失关键文案时会直接回退到基于正式菜谱文本重建的本地 prompt。
16. v0.55 起：图片评审工具默认优先读取 DOUBAO_REVIEW_MODEL；若未配置，则回退到 DOUBAO_TEXT_MODEL。单图页面会直接入选，多图页面会按页面类型套用不同评分维度，并把胜出图移入 publish。
17. v0.55 起：项目规范已收紧为“只要产生代码、脚本、配置或文档改动，就必须在任务结束前完成版本更新、git 提交与 GitHub 推送；若远端阻塞则必须记录原因”。
18. v0.56 起：tools/select_publish_images.py 无论是否 dry-run，都会在目标菜品目录下创建 publish 并写入 publish_selection_report.json / publish_selection_report.txt；dry-run 只是不移动图片，不再表现为“没有任何落盘产物”。
19. v0.57 起：首图与图解页的标题居中已升级为 publish 评审硬门槛；工具会结合中心参考线辅助图、本地像素偏移估算和豆包评审结果共同决定是否通过，当前默认 page01 容忍度更严。
20. v0.57 起：若标题页当前候选全部未通过居中硬门槛，正式模式会按原 prompt 单张补生并复评；dry-run 只报告“需要补生”。若补生被图片接口 403 阻塞，该页不会进入 publish，但整轮工具仍会继续完成其它页面并输出报告。
21. v0.57 起：main.py / image_generator.py 已接入自动 publish 收尾；PUBLISH_AUTO_SELECT=1 时，主流程会在 Photoshop 完成后自动执行发布图筛选，并把评分报告路径与入选图片路径回传。
22. v0.58 起：新增 tools/douyin_publish.py，通过 Playwright + Chrome 远程调试端口接管已打开的 creator.douyin.com 页面；脚本默认远程调试地址为 http://127.0.0.1:9222，若该端口未开启，则不会直接执行真实上传。
23. v0.58 起：抖音图文描述支持 PUBLISH_REQUIRED_TOPICS 手动必带话题；这些话题会优先占用最终 5 个标签名额，自动补位的话题继续过滤菜名标签与 #阿叶造新菜。
24. 本地已验证：image_generator.py 的手动话题注入逻辑可把配置话题带进最终 5 标签；tools/douyin_publish.py 的 py_compile 与 --dry-run 通过。当前机器未开启 http://127.0.0.1:9222，所以尚未做真实浏览器接管验证。
25. v0.59 起：tools/douyin_publish.py 在 CDP 连接失败时会直接输出更明确的中文原因，并说明脚本已经识别到图片与封面；同时启动前日志已改成“已识别上传图片”，避免和“没有识别到图片”混淆。
11. 本地 git 仓库已初始化，首个提交已完成。
12. GitHub 远程仓库已创建，仓库地址为 https://github.com/tradingdog/yiyechushi 。
13. 本地 main 分支已关联 origin/main。
14. 程序已支持将 guanggaoyu.txt 注入固定底部关注文案。
15. 当前主图模板方向已切换为截图1风格，并作为新流程的 VI 参考源。
16. 当前广告语变量内容为：关注@阿叶造新菜，家用开店都不赖！
17. 运行时配置优先级为：外部环境变量 > .env > config.env。
18. v0.08 起配方与步骤统一改用 g、ml、L 等物理量单位，并将高汤改为清水。
19. v0.09 起：新增 touxiang_generator.py，内置 10 个头像 prompt。
20. 主标题当前仍强制使用用户输入菜名，首图继续保留筷子夹起主菜的英雄镜头与原 VI 结构，真实拍摄约束只收敛到食材和局部照片层，不再放大到整张海报。
21. v0.11 起：文本接口超时时允许自动回退备用模型，仍失败则使用本地模板兜底继续出图。
22. v0.12 起：菜谱图生成完成后会继续生成独立封面图，封面 prompt 与封面图文件名会在菜名后追加“封面”。
23. v0.12 起：封面图强制使用统一 VI，底部是菜品虚化光圈背景，菜名按红色描边主标题样式纵向居中排布。
24. v0.13 起：新增 5 张独立多图解页面，固定顺序为食材怎么挑、关键细节拆解、省时技巧与工具、平替方案、口味调整。
25. v0.13 起：图解02到图解06分拆到 guide_pages 目录的独立脚本中，统一由 guide_generator.py 编排生成。
26. v0.14 起：图解01与封面图也分拆到 guide_pages 目录，分别由 page01_recipe.py 与 cover_page.py 维护页面逻辑。
27. v0.14 起：本地已完成 image_generator.py 修复后的 py_compile 校验，以及图解01、图解02到图解06、封面图的无 API 构造验证。
28. v0.15 起：图解01、图解02到图解06以及封面图的 prompt 模板已统一补充真实拍摄约束，强调自然偏暖、低饱和低对比、食材形态随机和去广告棚拍感。
29. v0.16 起：已修复“真实拍摄约束外溢导致整张图 VI 跑偏”的问题，当前要求恢复原有爆款海报 VI、标题字体、边框排版和英雄镜头，只把真实感限制在食材与局部照片内容层。
30. v0.17 起：图解02到图解06的本地兜底文案已改为读取当前 bundle 动态生成，支持不同 dish_name.txt 输入，不再固定为豆腐肉沫语义。
31. v0.18 起：图解02到图解04的 PAGE_DEFINITION 视觉焦点已去除豆腐肉沫豌豆固定词，图解04兜底文案残留固定句已清理，避免文生图 prompt 再次被固定菜名语义污染。
32. v0.19 起：新增 tools/check_prompt_pollution.py，一键检查图解02到图解06本地兜底文案与图解02到图解04 visual_focus 的固定菜词污染，并附带 py_compile 校验。
33. v0.20 起：文本调用链已与生图调用链分离，文本端默认走豆包（TEXT_API_PROVIDER=doubao，DOUBAO_TEXT_MODEL 默认 doubao-seed-2-0-pro-260215），生图端继续走 OpenAI gpt-image-2。
34. v0.21 起：主流程已调整为先产出全部创意与 prompt，再统一进入 gpt-image-2 生图阶段，避免边写 prompt 边出图。
35. v0.23 起：已用本地打桩验证主流程顺序，确认所有文案与 prompt 先完成后再统一进入图片生图阶段。
36. v0.24 起：图解01、图解02到图解06和封面图的图像内容约束已统一收口，只要出现任何菜品、食材、半成品、局部状态或背景食物，都必须写成 iPhone 主摄 1x 默认相机直出照片感，同时继续锁定原有海报 VI。
37. v0.25 起：图解01 首图模板单独补强了“先拍成真实晚饭照片再排进海报”的要求，新增晚饭室内光线、轻微自然虚化、少量必要道具和生活化悬空夹菜动作约束，用来压住首图主菜过假的问题。
38. v0.26 起：图解01 首图 prompt 的末尾固定追加“请优先保证主菜图片看起来像真实手机实拍，饱和度降低20%，再完成整张海报的文字和排版。”，确保这句以最后一行收尾。
39. v0.27 起：封面图模板和默认图片尺寸已统一改为竖版 2:3；模板里的账号昵称已改为“阿叶造新菜”；首页和图解02到图解06的图片 prompt 已统一接入“关注@阿叶造新菜，家用开店都不赖！”这句底部关注文案，且不生成图标。
40. v0.28 起：新增 config.env 作为统一可调参数文件，支持同一行中文注释；运行时配置优先级改为外部环境变量 > .env > config.env；默认图片模型更新为官方 GPT Image 2 最新快照 gpt-image-2-2026-04-21。
41. v0.29 起：主图、图解页、封面图与头像的默认图片质量统一调整为 medium；config.env 注释已补充 image_count 会同时影响成本与 IPM。
42. v0.30 起：新增 tools/generate_images_from_prompt_folder.py，可单独读取 tools/prompt 下的 txt prompt 文件并按 config.env 的 OPENAI_IMAGE_* 参数逐个生图；若没有 txt 文件则直接不生成。
43. v0.31 起：新增 generate_recipe_text_assets_from_idea_file() 作为“只到创意 txt 与文生图 txt 为止”的公共入口；tools/generate_text_prompts_from_dish_name.py 会基于 dish_name.txt 只生成文本和 prompt，不调用 gpt-image-2。
44. v0.32 起：封面模板只允许菜名和背景菜图；cover_page.py 已去掉整份 recipe_text 输入，并加强“除菜名外 0 文字”的硬约束，显式禁止引导句、副标题、黄条卖点、收藏提示、关注文案和任何小字。
45. v0.33 起：主流程文本阶段新增抖音发布文案生成，自动输出图文标题和图文描述，描述末尾固定带 5 个潜力话题标签并保存到 output/publish；tools/generate_images_from_prompt_folder.py 新增 --text-only 模式可单独生成这类文案。
46. v0.34 起：文本生成链路新增最小结构校验，创意菜谱、多图解文案、主图 prompt、封面 prompt 若返回空文本、占位文本或缺关键字段，会直接切本地兜底；save_text_output() 不再允许空文本落盘。
47. v0.35 起：图解01 首图新增“标题区严格水平居中”和“主菜照片必须作为整张主体背景图、不能套独立边框相框”的硬约束；图解02到图解06的标题和副标题也统一要求严格水平居中。
48. v0.36 起：chuantongcaipu.txt 已在原有烹饪方式分类下扩充到 743 道菜，新增 183 道国外传统菜，重点补足新马泰、日韩、印度、俄法意西、土耳其、中东和拉美经典菜，并已补入冬阴功汤、罗宋汤、海南鸡饭、肉骨茶、叻沙、西班牙海鲜饭等高频检索菜名。
49. v0.37 起：创意菜谱阶段的顶部引导句不再被“周末请客”“家宴”“请客就做”“有面子”这类老套模板强绑定；image_generator.py 已改成更动态的抖音式短钩子生成，并会在校验阶段自动替换掉这些陈词滥调。
50. v0.38 起：chuantongcaipu.txt 已改成“中餐 / 国外”双层分类结构，双层之下继续保留 15 个主烹饪方式分类；总量现为 939 道，其中中餐 560 道、国外 379 道，本轮新增 196 道中东、北非、东欧、拉美传统菜，且已校验无重复。
51. v0.39 起：新增自动造菜前置流程；AUTO_GENERATE_DISH_IDEA 默认开启时，主流程会先从 chuantongcaipu.txt 按菜系范围随机抽传统菜做参考，让豆包生成新的 dish_name.txt 内容，再继续生成图解 prompt 和图片。config.env 现已支持数字切换菜系范围，且新增 dish_idea_memory.jsonl 记录已用参考菜和历史新菜名，避免重复参考与重复新菜。
52. v0.40 起：图解01 首图不再默认固定为木桌、暖陶盘、木托和小碗陪衬；主画面说明已扩展出“器皿与摆盘 / 桌面与环境 / 背景陪衬”三层信息，并在 image_generator.py 中按菜名和做法自动推断，再由 page01_recipe.py 强制要求首图 prompt 跟随这些字段生成更贴菜的出餐场景。
53. v0.41 起：normalize_recipe_text() 不再无条件覆盖豆包产出的“器皿与摆盘 / 桌面与环境 / 背景陪衬”；只有字段缺失或明显占位时，才会回退到本地补齐。
54. v0.41 起：首图本地场景推断新增场景语句提取，只让摆盘、装盘、上桌、桌面、台面等相关语句参与器皿与桌面判断，避免被“淀粉”“白胡椒粉”“蒸锅”“脆甜”等泛词误导到炸物盘、面碗石面或固定白色水磨石模板。
55. v0.42 起：引导句和副标题若出现“完全相同”“只差空格”或“只多减 1 到 2 个字”的近似重复，会在 validate_recipe_text_content() 阶段自动重写顶部引导句，确保上方是钩子、下方是卖点词。
56. v0.43 起：多图解图片 prompt 不再允许混入“当前菜名 / 当前页面 / 页面名称 / 页面标题 / 页面副标题 / 内容卡1-3 / 页尾提示”等程序化字段标签；共享模板会先把图解文案转成只含实际展示文字值的摘要，再进入图片 prompt 阶段，且 image_generator.py 新增了图解 prompt 专用拦截校验。
57. v0.44 起：封面背景不能再独立走泛化暖色虚化模板；guide_pages/cover_page.py 现在必须沿用首图同一道菜的器皿、桌面、背景陪衬、主画面食材和酱汁状态，image_generator.py 也新增封面 prompt 场景锁定校验，缺少这些字段就直接切本地模板。
58. v0.45 起：首图归一化阶段会识别豆包生成的模板化场景字段；若“桌面与环境”或“背景陪衬”落回浅灰石面 / 水磨石 / 简洁干净 / 小碟失焦等常见模板，就自动改用本地更贴菜的场景推断。与此同时，本地场景推断已新增“酸汤/木姜子类煎鱼片”和“黑椒/春卷皮类脆煎里脊卷”分支，主动拉开不同结构菜的首图环境。
59. v0.46 起：封面图的菜名竖排不能只写“垂直居中”；cover_page.py 现在会明确要求“画布正中保留一整条竖向标题通道，菜名单列竖排压在中轴线上”，并要求背景主菜、餐盘和陪衬退到下半部或左右下角，避免把竖排菜名挤到左边。image_generator.py 也新增了封面中轴约束校验，缺少这两层语义就直接切回本地封面模板。
60. v0.47 起：主流程与单独调试生图链路的输出目录都已改为 output/日期时间戳_菜品名 单文件夹模式；图解文案、图解 prompt、封面 prompt、最终图片、revised prompt、抖音图文标题与图文描述都会放进同一个运行目录，不再继续拆 output/chuangyi、output/prompt、output/image、output/publish 子目录。
61. v0.48 起：封面图比例已从竖版 2:3 切换到竖版 9:16；cover_page.py 的封面模板明确要求使用更高更窄的长竖版构图，上半段给竖排菜名留更长通道，主菜和餐盘更多落到中下段，避免继续沿用旧版 2:3 的填满式背景布局。
62. v0.49 起：图解01 首图标题区不能只写“严格水平居中”；page01_recipe.py 现已追加“沿同一条画面正中竖线向下堆叠的中心柱布局”硬约束，要求引导句、主标题、黄条卖点和收藏提示条的中心点全部落在同一条中轴线上，并显式禁止任何一行从左边起排。image_generator.py 也新增了首图 prompt 专用中轴校验，缺少中轴、中心点、左右留白对称或禁止偏左语义时，会直接切回本地首图模板。
63. v0.50 起：OpenAI 官方当前对 gpt-image-2 的尺寸约束已确认是“最长边不超过 3840、宽高都必须是 16 的倍数、总像素介于 655360 和 8294400 之间、长短边比例不超过 3:1”。项目已把封面默认尺寸从非法的 1080x1920 改为合法的 2160x3840，并在 image_generator.py 里给主图、图解页和封面图统一加上本地尺寸校验，避免再把错误尺寸直接送进 API。
64. v0.51 起：新增 tools/apply_photoshop_template_batch.py，作为独立 Photoshop 云 API 批处理工具；脚本会把指定目录下的 .jpg/.jpeg/.png 上传到 S3，经 Photoshop 云 API 替换进 PSD 模板的命名智能对象层，再导出高质量平面 JPG 覆盖原图。
65. v0.51 起：PSD 模板默认路径为 tools/photoshop_template/template.psd，默认智能对象层名称为 input_image；模板里的混合模式、文字层、纹理层与调整层会原样保留，自然饱和度等后期调整建议直接做进 PSD 模板。
66. v0.51 起：config.env 已新增 Adobe Firefly Services 与 S3 中转配置项；本地已验证新工具 py_compile、错误检查和 --help 命令输出均正常，但完整端到端测试仍需要用户提供 Adobe 凭证、S3 桶和 PSD 模板。

## 当前阻塞与待办

1. 继续调优截图1模板下的主菜真实性和标题转化文案。
2. 后续可继续调优图解02到图解06的页面密度与信息节奏。
3. 后续可继续校准抖音标题卖点的变化度，避免不同菜频繁落回相似句式。
4. 后续可继续校准创意菜谱阶段的原创度与商用可落地度平衡。
5. 后续每次目录有改动，都需要同步更新版本、提交并推送远程仓库。
6. 本地 Photoshop 模式要求运行前先关闭 Photoshop，避免工具误关闭用户当前正在编辑的工作区。
7. 抖音与巨量公开页存在签名或 JS 渲染拦截，当前不适合把伪实时外站抓取直接挂进主流程；话题选择先走稳定的通用高频话题池。
