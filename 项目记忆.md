# 项目记忆

## 当前版本

v0.10

## 当前阶段

第一阶段：完成从菜品创意到一页菜谱图片的完整生成链路。

## 当前目录结构要点

1. main.py 负责启动生成流程。
2. image_generator.py 负责读取 dish_name.txt、生成创意菜谱、生成文生图 prompt、调用 gpt-image-2 并保存图片。
3. touxiang_generator.py 负责批量生成 10 个头像。
4. dish_name.txt 第一行是用户菜名或创意，第二行起是补充说明。
5. 临时调试prompt.txt 存放当前截图1风格的固定 VI 参考模板。
6. guanggaoyu.txt 负责提供关注文案变量。
7. output/chuangyi 目录保存每次生成的创意菜谱文本。
8. output/prompt 目录保存每次生成的文生图 prompt。
9. output/image 目录保存每次生成的菜图。
10. touxiang 目录由程序运行时自动创建并保存头像。

## 已确认事项

1. OpenAI 图片模型默认使用 gpt-image-2。
2. 文本生成默认使用 OPENAI_TEXT_MODEL，.env.example 当前示例值为 gpt-4.1-mini。
3. 默认图片尺寸为 1024x1536，对应竖版 2:3。
4. 默认图片质量为 high。
5. 文件名需带日期和时间戳。
6. main.py 当前会按“创意菜谱 -> 文生图 prompt -> 最终图片”顺序执行。
6. 本地 git 仓库已初始化，首个提交已完成。
7. GitHub 远程仓库已创建，仓库地址为 https://github.com/tradingdog/yiyechushi 。
8. 本地 main 分支已关联 origin/main。
9. 程序已支持将 guanggaoyu.txt 注入固定底部关注文案。
10. 当前主图模板方向已切换为截图1风格，并作为新流程的 VI 参考源。
11. 当前广告语变量内容为：关注@一页厨，一页一道新菜，家用开店都不赖。
12. v0.08 起默认每次生成 2 张图。
13. v0.08 起配方与步骤统一改用 g、ml、L 等物理量单位，并将高汤改为清水。
14. v0.09 起：新增 touxiang_generator.py，内置 10 个头像 prompt。
15. v0.10 起：新增 output 三段式落盘目录，主流程改为直接读取 dish_name.txt 自动生成完整一页菜谱。

## 当前阻塞与待办

1. 继续调优截图1模板下的主菜真实性和标题转化文案。
2. 后续可继续沉淀一页厨头像方向，筛选出长期品牌头像。
3. 后续可继续校准创意菜谱阶段的原创度与商用可落地度平衡。
4. 后续每次目录有改动，都需要同步更新版本、提交并推送远程仓库。
