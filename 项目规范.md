# 项目规范

## 当前版本

v0.11

## 版本规则

1. 只要本目录发生新增、修改、删除，就视为一次版本变更。
2. 版本号从 v0.01 开始，每次变更递增 0.01，例如 v0.02、v0.03。
3. 每次版本变更后，必须同步更新 版本记录.md 和 项目记忆.md。
4. 若存在代码或文档变更，优先先完成版本更新，再进行 git 提交与推送。

## Git 与 GitHub 规则

1. 提交标题格式固定为：v版本号：中文说明。
2. 所有提交说明、变更说明、版本记录，主要使用中文。
3. 只要有目录改动，默认流程为：更新版本号 -> 更新文档 -> git 提交 -> 推送 GitHub。
4. 若远程仓库或 GitHub 登录未完成，先完成本地提交，并在 项目记忆.md 记录阻塞原因。

## 语言规则

1. 说明文档、注释、输出信息优先使用中文。
2. 遇到必须使用英文的技术字段、库名、接口名，再保留英文。

## 第一阶段目录约定

1. main.py 为主程序入口。
2. image_generator.py 为完整生成模块脚本，负责创意菜谱、文生图 prompt 与最终图片输出。
3. touxiang_generator.py 为头像批量生成脚本。
4. dish_name.txt 为本次菜名创意输入文件，第一行是菜名或创意，第二行起是补充说明。
5. 临时调试prompt.txt 为当前固定 VI 风格参考 prompt。
6. guanggaoyu.txt 为关注文案变量文件。
7. output/chuangyi 用于保存每次生成的创意菜谱文本。
8. output/prompt 用于保存每次生成的文生图 prompt 与 revised prompt。
9. output/image 用于保存最终菜谱海报图。
10. touxiang 目录用于保存头像生成结果。
11. .env 保存本地环境变量，不进入版本库。
