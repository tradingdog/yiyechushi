# 项目规范

## 当前版本

v0.80

## 版本规则

1. 只要本目录发生新增、修改、删除，就视为一次版本变更。
2. 版本号从 v0.01 开始，每次变更递增 0.01，例如 v0.02、v0.03。
3. 每次版本变更后，必须同步更新 版本记录.md 和 项目记忆.md。
4. 若存在代码或文档变更，优先先完成版本更新，再进行 git 提交与推送。
5. 任何一次任务只要产生代码、脚本、配置或文档改动，在结束前都必须完成 git status 检查、版本更新、本地提交与推送 GitHub；除非用户明确要求不推送，或远端出现客观阻塞。

## Git 与 GitHub 规则

1. 提交标题格式固定为：v版本号：中文说明。
2. 所有提交说明、变更说明、版本记录，主要使用中文。
3. 只要有目录改动，默认流程为：更新版本号 -> 更新文档 -> git 提交 -> 推送 GitHub。
4. 若远程仓库或 GitHub 登录未完成，先完成本地提交，并在 项目记忆.md 记录阻塞原因。
5. 若本地存在未提交改动，不得直接结束任务；必须先明确这些改动是否纳入当前版本，并完成提交与推送或记录阻塞原因。

## 语言规则

1. 说明文档、注释、输出信息优先使用中文。
2. 遇到必须使用英文的技术字段、库名、接口名，再保留英文。

## 第一阶段目录约定

1. main.py 为主程序入口。
2. image_generator.py 为完整生成模块脚本，负责通用素材整理、接口调用、本地兜底与总流程编排。
3. guide_generator.py 负责编排图解02到图解06的文案、prompt 与出图流程。
4. guide_pages 目录用于存放图解01到图解06以及封面图各自独立的页面脚本。
5. dish_name.txt 为本次菜名创意输入文件，第一行是菜名或创意，第二行起是补充说明。
6. 临时调试prompt.txt 为当前固定 VI 风格参考 prompt。
7. guanggaoyu.txt 为关注文案变量文件。
8. output 目录用于承载所有主流程输出结果。
9. 每次运行时，程序都要在 output 下按“日期时间戳_菜品名”创建单次输出文件夹。
10. 该单次输出文件夹内需统一保存图解文案、图解 prompt、封面 prompt、最终图片、revised prompt、抖音图文标题与抖音图文描述，不再拆分子目录。
11. tools/apply_photoshop_template_batch.py 为主流程和独立调试共用的本地 PSD 模板后期合成工具；当 PHOTOSHOP_AUTO_COMPOSITE=1 时，main.py 会在全部图片落盘后自动调用。
12. tools/photoshop_template 目录用于存放 PSD 模板，默认文件名为 template.psd。
13. Photoshop 自动合成开关统一使用 PHOTOSHOP_AUTO_COMPOSITE：1 开启，2 关闭。
14. PHOTOSHOP_LOCAL_EXE 必须指向 Photoshop.exe；若运行时检测到 Photoshop 已经打开，应先手动关闭后再执行。
15. run_photoshop_template_batch.cmd 为 Windows 双击启动脚本，可直接列出 output 子目录并调用本地 Photoshop 批处理工具。
16. tools/select_publish_images.py 用于把某个 output 子目录里的多版本图片投喂给豆包做页面级评审，再把每页胜出图移动或复制到 publish 文件夹。
17. 首图与图解02到图解06的标题居中是 publish 评审硬门槛；若当前候选全部未通过，工具会按该页已经落盘的文生图 prompt 单张补生后复评，补生上限由 PUBLISH_TITLE_CENTER_RETRY_LIMIT 控制。
18. 主流程在 PUBLISH_AUTO_SELECT=1 时，会在整套图片生成并完成 Photoshop 合成后自动创建 publish、筛选发布图并输出评分报告。
19. .env 保存本地环境变量，不进入版本库。
20. tools/douyin_publish.py 用于接管已开启远程调试的 Chrome 抖音创作者页，自动上传 publish 图片、填写标题与描述、单独上传封面并完成自主声明选择；默认远程调试地址是 http://127.0.0.1:9222。
21. PUBLISH_REQUIRED_TOPICS 用于配置抖音图文描述最后 5 个话题里的手动必带标签；多个话题可用空格、逗号、分号或换行分隔，写不写 # 都可以。
22. v0.64 起，main.py 在当前主流程最后一步完成并输出本轮结果后，若 output 子目录下已生成 publish 文件夹，会继续调用 tools/douyin_publish.py 作为主流程收尾步骤，自动把 publish 图集、标题、描述与封面接到抖音创作者页，并默认停在人工发布前。
23. v0.65 起，文本阶段不再允许本地模板兜底；单个文本阶段若超时或连续返回内容异常，会继续用当前同一模型最多重试 3 次，超过后直接终止本轮流程。
24. v0.67 起，豆包/OpenAI 文本链路的等待时间按 2026-05-26 当天实测收口到 120 秒；当前豆包 doubao-seed-2-0-mini-260428 在真实创意菜谱与首图 prompt 请求中的返回耗时主要落在 12 到 45 秒。首图 prompt 阶段若出现“内容异常”，优先排查本地 validator 是否把等义居中表述或空格差异误判为失败，而不是先继续盲目上调超时。
25. v0.69 起，文本请求的自动重试不再只认“timed out”字样；httpx/openai 的连接中断、空消息超时和其它传输层瞬时异常也会进入同一模型重试。重试时会重建文本 client，并把 connect/write/pool 阶段单独收紧到较短超时，避免主流程偶发卡死后只能整轮重跑。
26. v0.70 起，封面 prompt 的 validator 也按等价表达放宽：连续菜名检测与首图同场景锁定字段，都改为忽略空格和标点后的等价匹配。像“豆、腐、锅、巴、虾”这类竖排拆字写法，或“器皿是米白色圆形骨瓷盘，盘底铺完整脆锅巴”这类仅改了逗号/停顿的场景描述，不再被误判成封面内容异常。
27. v0.71 起，封面 prompt 若仍缺 9:16、中轴竖排、背景避让或首图同场景锁定字段，程序会像首图 prompt 一样把这些硬约束直接补回最终 prompt 再校验，而不是继续把语义方向正确但措辞不够全的封面返回当成失败。
28. v0.72 起，图解01 一页菜谱必须单独读取 OPENAI_IMAGE_MODEL / OPENAI_IMAGE_SIZE / OPENAI_IMAGE_QUALITY / OPENAI_IMAGE_COUNT；只有图解02到图解06才读取 OPENAI_TUJIE_IMAGE_*。主流程日志也要分别打印“首图模型 / 图解模型 / 封面模型”，避免把首图误跑成图解那套 medium + 3 张配置。
29. v0.73 起，封面图默认尺寸收口为 864x1536；继续保持竖版 9:16，但像素规格不再单独拉满。封面尺寸应维持与首图、图解页接近的中等分辨率，避免高规格封面把构图和主体比例进一步推向失衡。
30. v0.74 起，图解01 首图上半部不再额外放“收藏提示”细条；当前顶部只保留引导句、主标题和黄条卖点，底部继续保留收藏关注横条。抖音图文描述模板也不再允许统一写成“这道原创融合的”“先收藏，想做时……”这类机械开头或收尾。
31. v0.75 起，图解01 首图动作不再默认固定成“筷子夹起主菜 + 滴汁挂汁”模板；当前只要求有筷子夹起主菜的动作感，角度、方向、瞬间和构图距离都可随菜品变化，并允许少量年轻手部自然入镜。主流程在 PUBLISH_AUTO_SELECT=1 时也改为先筛前 6 组、再按筛中的首图重写并生成封面，最后只对 publish 目录里的入选图片做 Photoshop 合成。
32. v0.76 起，文本接口的可重试异常判断必须沿异常链识别底层 TLS/SSL EOF、httpcore NetworkError/ProtocolError 与超时，而不能只看最外层异常或简单字符串；首次请求若只是瞬时网络抖动，必须在同轮自动重建文本 client 并重试，不允许再把“第一次报错、第二次手动重跑才好”当成可接受行为。
33. v0.77 起，图解02到图解06的 prompt 校验不能再沿用首图/封面那种“缺一个硬词就直接失败”的思路；延续页 prompt 允许不显式写出完整菜名，也允许在说明语境里提到“页面标题”“内容卡”这类结构词。当前应优先把缺失的菜名锚点和“不要出现程序化字段标签”负约束补回最终 prompt，而不是直接判死。
34. v0.78 起，首页和其它出现主菜的页面都不能再默认固定成“一双木筷夹起主菜”的互动模板；餐具类型、餐具数量、是否一只手或多只手入镜，以及具体互动动作，都必须由模型按菜品结构、上桌逻辑和吃法自行判断。共享 prompt 应至少覆盖勺子/汤匙、刀叉、筷子、夹子、锅勺、分餐勺等合理组合，并允许舀汤、切开、叉起、捞起、翻面、分勺、蘸汁、卷起等不同动作瞬间。
35. v0.79 起，首页底部步骤区不能再默认固定成“5步出锅”；文本模板、解析器、validator 和首图 prompt 都必须接受并正确处理【3步出锅】、【4步出锅】和【5步出锅】三种结构，按菜品保留最刚好的步骤数量，越少越好，但最多只能 5 步。图解06 的视觉引导也不能再默认每套都做成旋钮或滑杆，必须允许更开放的数值表现形式。
36. v0.80 起，publish 标题居中补生图只允许写入临时评审目录并在流程结束后自动清理，不得再直接落到 output 根目录污染原始候选张数；首图补生参数必须读取 OPENAI_IMAGE_*，不能误用图解页 OPENAI_TUJIE_IMAGE_*。
