# V2 项目记忆

## v0.27

- 用户反馈：看图栏信息卡中两个字段都显示“历史记录”，语义不明确，无法区分“参考菜”和“菜系”。
- 根因定位：历史卡片点击事件里把 `参考菜/菜系` 两个值硬编码为同一文本“历史记录”。
- 已修复：历史预览改为按字段显示缺省文案“未记录参考菜”“未记录菜系”，不再出现重复且误导性的同值展示。

## v0.26

- 用户需求：去掉“常用菜快速生成”模块，并澄清“自动造菜 + 补充说明”的真实生效逻辑。
- 已落地：删除快捷菜模块及对应点击即运行逻辑，避免误触后直接发起任务。
- 逻辑确认：补充说明仅在手动点名时写入 `dish_name.txt` 并传入豆包；自动造菜模式不会读取该输入作为约束。

## v0.25

- 用户需求：本地电脑开机后自动启动 V2 服务，进浏览器即可直接访问，不想每次手动运行脚本。
- 落地方案：优先尝试计划任务自启，但当前环境被系统权限拒绝；改为“用户启动文件夹自启”方案，无需管理员权限。
- 已完成：新增 `start_web_panel.bat`（含重复进程检测 + 隐藏启动）并写入启动项 `V2_WebPanel_AutoStart.cmd`，保证登录后自动拉起 `web_panel.py`。

## v0.24

- 用户反馈：看图栏顶部切图区出现“半截显示”，缩略图与按钮区不完整。
- 已修复：将切图控件放入独立容器并固定最小高度，改为上按钮下缩略图的双层布局，避免被后续区域挤压。

## v0.23

- 用户反馈：切图控件在看图栏下方，必须滚轮下移才能操作，不够顺手。
- 已修复：把上一张/下一张与缩略图选择整体上移到看图栏顶部，进入看图区即可直接切图。

## v0.22

- 用户反馈：历史输出卡片里按钮和菜名存在重叠，影响阅读与点击。
- 已修复：重构历史卡片为分区布局，操作按钮独立放到底部操作栏，不再遮挡缩略图或文字信息。

## v0.21

- 用户反馈：看图栏存在遮挡（运行态覆盖主图）且图片默认适配不佳。
- 已修复：运行中若已有图片仅显示角标进度，不再盖住整张图；主图适配策略改为容器内完整自适应显示。

## v0.20

- 用户反馈：结果信息卡覆盖在图片上，影响看图体验。
- 已修复：信息卡移出图片区域，改到图片下方展示；样式改为紧凑信息条，确保图片完整可见。

## v0.19

- 用户反馈：当前改版后仍不够顺手，明确要求三栏结构（左历史、中控制、右看图）且允许自行拖拽调整宽度并记忆。
- 已落地：新增三栏布局与双分隔条拖拽，默认右侧看图栏更宽；历史输出迁移到左栏并改为目录风格（缩略图+菜名）。
- 已落地：分栏宽度写入本地存储，刷新或重开页面后自动恢复用户上次调整结果。

## v0.18

- 用户需求：用图2作为网页图标，解决标签页无 favicon 的问题。
- 已落地：复制图2为 `V2/favicon.png`，前端 `head` 增加 icon 链接，服务端补齐 favicon 路由用于浏览器自动请求。

## v0.17

- 用户明确偏好深色界面，并给出完整交互建议：3:7 布局、抽屉日志、卡片历史、运行按钮进度、快捷生成与导航按钮分组。
- 已落地：前端重排为左侧固定控制 + 右侧结果历史；颜色、背景、层级切换为深色模式，顶部操作按钮与状态信息集中展示。
- 已落地：历史区域升级为双列图片卡片并支持悬停操作，日志改成可折叠抽屉（清空/只看错误），整体操作流程改为“左调参、右看图、底部按需看日志”。

## v0.16

- 用户反馈关键问题：当上一轮（如“焖酿蹄花”）图片还未返回时，前端再发起新任务，上一轮像“石沉大海”，无法确认是否继续执行。
- 根因定位：原面板是“单任务即时执行 + 覆盖式状态”，新请求要么被拒绝、要么覆盖当前可见状态，用户感知上像上一轮被吞掉。
- 已修复：后端改为任务队列顺序执行，新任务统一入队；运行态可继续“加入队列”，状态与日志显示队列和任务编号，确保每轮都有执行与可追踪反馈。

## v0.15

- 用户新增要求：双击图片要能新标签页打开；结果区红框信息不能空白；默认无图状态要改为美观提示；整体视觉参考 OpenAI 页面风格。
- 已落地：主图与缩略图都支持双击新标签打开；历史记录返回菜名并在右侧结果区同步展示，空值统一为中文可读占位。
- 已落地：无图/加载失败引入专用占位态（含图标与说明），避免浏览器破图观感；页面配色与顶部结构改为深色头部 + 浅色内容区的简洁布局。

## v0.14

- 用户痛点：当前面板“非人性化、难操作”，尤其图片一多后难以分辨；并要求前端去掉英文展示、增加版本号用于识别。
- 已落地：重做页面色调与信息层级，采用主图固定区 + 缩略图网格 + 历史卡片预览图，降低多图浏览认知负担。
- 已落地：页面文案改为中文优先，不再向用户暴露英文组件信息；顶部新增 `版本` 标签，当前为 `v0.14`。

## v0.13

- 用户反馈：第一版网页面板“从专业网页角度体验不佳”，希望从多维度自评并优化。
- 已完成：围绕信息层级、可读性、交互反馈、容错和移动端适配做第二版 UX 升级，保持后端调用链不变。
- 当前面板重点提升：运行状态更可感知（秒级计时）、手动模式校验更明确、历史预览可定位、操作路径更短。

## v0.12

- 用户需求：先看一版“UI 友好、方便人类操作”的 V2 前端网页面板。
- 已实现：`V2/web_panel.py` 内置单文件前端页面 + 本地 HTTP API，直接调用 `run_v2_first_feature()`，无需改动主流程入口。
- 首版能力：模式切换、手动菜名输入、温度/质量/数量调参、一键运行、结果与图片预览、最近输出历史。

## v0.11

- 用户要求：以后无需手动点 Review/Commit，V2 流程由 AI 自动同步仓库。
- 已落地：规范中新增“默认自动执行 `git status -> commit -> push`”的硬规则，并保留“用户明确禁止或远端阻塞”例外。

## v0.10

- 用户问题确认：V2 自动造菜缺少历史创意记忆与参考菜追溯，导致近期结果口味方向重复（如连续“蒜香”）。
- 已修复：V2 自动造菜直接复用 V1 核心函数，启用 V1 同款反重复策略与历史禁用约束。
- 已落地：新增 `V2/dish_idea_memory.jsonl`，并在日志中打印参考传统菜和记忆文件路径，方便排查“由哪道传统菜衍生而来”。

## v0.09

- 新要求确认：只有含 `{变量}` 的位置可以改写；未含变量的模板行必须保持 100% 原文。
- 已落地：豆包不再输出整段 prompt，而是仅输出变量替换数组；程序负责把替换值填回原模板。
- 结果：模板固定文本（尤其 `文案` 行）不会被模型改写，避免调试过的 prompt 被意外污染。

## v0.08

- 补充修正：v0.07 的重排虽然生效，但在少数单段输出里会把“真实拍摄”误判为“拍摄分类”起点，导致分段错位。
- 已修复：分类关键词匹配增加中文标点/空白边界约束，仅在更像“新字段起始”的位置触发。
- 同步增强：豆包提示词生成阶段新增“11 分类标签 + 空段”硬约束，优先从源头保障可读结构。

## v0.07

- 用户诉求：提示词文件需要像 `cankao.txt` 一样按分类分段，便于人类阅读和后续优化。
- 已实现：新增 `format_prompt_by_template_sections()`，先按模板抽取分类标签，再从模型输出中回填对应内容，最终按“分类 + 空行”格式落盘。
- 兼容性：支持模型返回单行长句、半结构化文本和模板化文本三类输出形态。

## v0.06

- 用户困惑点：`AUTO_GENERATE_DISH_IDEA=1` 时怀疑未自动造菜，以及“已有图片但终端出现失败信息”。
- 诊断结论：`20260601_031028_main_32364.log` 已确认该轮实际是自动造菜并成功生图；此前失败信息来自旧目录旧轮次，且 V2 存在多段重试分支容易造成感知混乱。
- 已修复：输出日志增加“开关值/命令行模式/最终模式”；生图流程改为更接近 V1 的单路径逻辑，仅保留超时重试。

## v0.05

- 新增根因：`V2/config.env` 中 `OPENAI_BASE_URL=` 空值会把环境变量设置为空字符串，OpenAI SDK 读取后会报 `UnsupportedProtocol`（上层表现为 `Connection error`）。
- 已修复：V2 生图客户端在空值场景强制使用官方默认 `https://api.openai.com/v1`。

## v0.04

- 进一步定位：同机直测 `OpenAI(api_key, timeout)` + `gpt-image-2` 能成功返回图片，排除 key 错误与官方网络不通。
- 根因收敛：V2 先前为生图客户端注入自定义 `httpx.Client`，与 V1 行为不一致，导致在当前环境下偶发/持续 `Connection error`。
- 已修复：V2 生图客户端改回 V1 同款初始化，仅保留 `api_key/timeout`（和可选 `base_url`）。

## v0.03

- 排查结果：`OPENAI_API_KEY` 读取变量名与 V1 一致，均为 `OPENAI_API_KEY`，并来自根目录 `.env`。
- 关键差异：V2 先前显式 `HTTP_TRUST_ENV=0`，导致不继承系统网络环境；在当前机器上该模式访问官方 OpenAI 会超时。
- 已修复：默认改为 `HTTP_TRUST_ENV=1` 并同步配置文件，现与 V1 的网络行为一致。

## v0.02

- 问题复盘：当豆包改写失败走本地降级模板时，旧正则仅匹配 `{变量}` 与 `{变量：...}`，未覆盖 `{变量，...}`，导致个别行残留占位符。
- 已修复：统一占位符规则为 `TEMPLATE_PLACEHOLDER_PATTERN`，降级替换和结果校验共用同一规则，避免再次漏替换。
- 结果：`cankao.txt` 中含中文逗号注释的变量位也能被按最新菜名语义替换。

## v0.01

- 目标：把 V2 做成与根目录主流程并行的独立版本，先实现首个功能闭环。
- 已实现：`AUTO_GENERATE_DISH_IDEA` 控制自动造菜或手动菜名读取。
- 已实现：读取 `cankao.txt` 后由豆包补全 `{变量}` 并生成最终提示词 txt。
- 已实现：把最终提示词直接投喂 `gpt-image-2` 生图，输出到 `V2/output/时间戳_菜名`。
- 默认配置：`OPENAI_IMAGE_QUALITY=low`，`OPENAI_IMAGE_COUNT=1`。
