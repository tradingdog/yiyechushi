# V2 项目记忆

## v1.45

- **快手图文定时发布 DOM（cp.kuaishou.com）**：Ant Design `DatePicker`（`input[placeholder="选择日期时间"]`，格式 `yyyy-MM-dd HH:mm:ss`）。点 label「定时发布」（`input[type=radio][value="2"]` 隐藏），弹层 `.ant-picker-dropdown` 选 `.ant-picker-cell-inner` 日期、三列滚轮选时/分/秒，**必须点 footer `.ant-btn-primary`「确定」** 关闭弹层；Playwright 可用 `click(force=True)` 点确定按钮。
- 面板定时发布统一传 `schedule_at` 给 `douyin` 与 `kuaishou` 子进程；其它平台忽略。

## v1.44

- 面板发布栏「定时发布」仅向抖音子进程传 `--schedule-at`；`resolve_settings` 有 schedule 时自动 `auto_submit_publish=True`。
- Playwright 定时逻辑在 `tools/douyin_publish.py` 的 `set_scheduled_publish_time`：label 点选、Semi DatePicker 滚轮选时、输入框右侧空白 dismiss；勿点「立即发布」label。

## v1.43

- 发布计划缩略图缺失根因：v1.42 `collectPlanAssignedPaths` 返回 normalize 路径，但槽位存的是 `D:\...` 原始路径；`hydratePublishPlanCache` 用 normalize key 写缓存、`createPlanSlotChip` 用原始 key 读 → 对不上。修复：计划比对用 `collectPlanAssignedPathsNormalized`，缓存用 `rememberHistoryCacheItem` / `getHistoryCacheItem` 双键。
- **抖音图文定时发布 DOM（creator.douyin.com）**：Semi `DatePicker`（`input[placeholder="日期和时间"]`，格式 `yyyy-MM-dd HH:mm`）。先点 label「定时发布」（`input[value="1"]` 为 opacity:0 假 radio），再开 picker 选日期格 `.semi-datepicker-day`、点 `.semi-datepicker-switch-time` 进滚轮，时/分分别在 `.undefined-list-hour` / `.undefined-list-minute`（无限列表，需按当前选中项附近 scroll+click）。**选完必须点弹层外空白确认**（如描述区、预览区等非 picker 区域），弹层关闭且输入框仍显示目标时间才算生效；Playwright 可用 `page.mouse.click(x,y)` 点日期输入框右侧空白或上方表单区。

## v1.42

- 新造菜拖不进发布计划根因：有预览图的卡片从缩略图起手拖会触发 **img 原生拖拽**，父级 `dragstart` 不写 `application/x-dish-path`，drop 读不到路径。生菜/无图旧项从文字区拖仍正常。修复：`draggable="false"`、`pointer-events:none`、路径双写 text/plain。

## v1.41

- v1.39/v1.40 把 ≤10 MiB 的 1K 参考图也压到 768 短边 JPEG，参考效果变差。现改为**仅体积超限才压缩**：`images.edit` 阈值 50 MiB；豆包 `image_url` 阈值 9 MiB。4K 13 MiB 仍会压，1K 2.3 MiB 原样传。

## v1.40

- 自定义生图「无反应」根因：任务失败后 slot 仍为 pending，历史 UI 一直显示「生成中」；且 worker 日志不进 `RUN_LOG_LINES`，底部日志空白。现独立 `CUSTOM_IMAGE_LOG_LINES` 并 poll 合并展示。
- 参考图生图 timeout：4K PNG 参考图直传 `images.edit` 易慢/超时；`prepare_reference_image_for_edit_api` 先压到 ≤2048 长边 JPEG。
- 面板重启后 `history.json` 里 status=running 的 job 在 `init_custom_image_service` 标 failed，避免假运行态。

## v1.39

- 4K PNG（如 2336×3504）常达 12–15 MiB，豆包 `image_url` 硬限 **10 MiB**（非 OpenAI 的 20 MiB）。缺陷审核、选图、气泡/平台文案均走 `encode_image_as_data_url`，须在编码前压缩。
- OpenAI Vision 会预处理为长边 ≤2048、短边 ≤768；`images.edit` 参考图 multipart 上限 50 MiB，4K 原图可直接传，不必为 edit 降分辨率。

## v1.38

- 官方批量生图根因：`config.env` 里 `OPENAI_BASE_URL` 常为丝路地址，旧 `get_image_api_batch_n` 在 provider=official 时仍按 base_url 判丝路 → 强制 n=1。现 provider 优先，official 直接 `min(batch,4)`。
- **停止** 与 **终止** 区分：停止只清队列+设 `WORK_CANCEL_EVENT`+杀发布子进程，不关 HTTP 面板；终止/重启走 `schedule_panel_shutdown`。
- 后台线程无法强杀，取消靠 `raise_if_work_cancelled` / `sleep_with_cancel` 在重试等待与生图循环检查点抛出。

## v1.37

- 顶栏下拉菜单被 `overflow-x:auto` 裁成细条，用户只能点滚动箭头。已废除下拉，画质/比例改分段按钮；顶栏本体 `overflow:visible`，仅 toolbar 横向滚动。

## v1.36

- 顶栏生图控件改单行紧凑布局：禁 `flex-wrap` 换行；画质/比例用 `.top-drop` 弹出菜单；隐藏 `<select>` 仅作状态同步。窄屏顶栏可横向滚动。

## v1.35

- 菜品池空白根因：v1.34 合并时 JS 里 `IMAGE_GEN_PREFS_STORAGE_KEY` 与整段生图控件函数重复粘贴，浏览器 `SyntaxError` 后 `loadState`/`loadHistory` 均未执行。勿在同一 `<script>` 块重复 `const` 声明。

## v1.34

- 顶栏生图默认 **官方**（`OPENAIOFFICIAL_API_KEY` → `api.openai.com`）；丝路仍用 `OPENAI_API_KEY` + `OPENAI_BASE_URL`。`build_openai_image_client` 按 `IMAGE_API_PROVIDER` 分流，不再混用 key。
- 尺寸表在 `image_gen_profile.py`；2:3 的 1K 固定 **1024×1536**（兼容现有 `template.psd`）。升 2K/4K 须同步做对应 `template_{宽}x高}.psd`，**禁止**再靠合成前缩放凑尺寸（v1.31 教训）。
- 丝路顶栏仅 1K；官方可选 2K/4K。`get_image_api_batch_n` 也按 provider 判断，官方可 n>1。

## v1.33

- 看图栏「非发布图」可 **替换** 发布图：多选候选 → 执行后 `copy` 到 `publish/`（不移动原图）→ 删对应 `final` 槽位 → 仅对替换类型跑 PS → `apply_final_image_sequence_names`。文件名须含 海报/细节/菜谱/封面 关键词；同类型只能选一张。后端 `run_publish_image_replace`（`mode2_flow.py`）。

## v1.32

- 2336×3504「放大」实验作废：丝路 `gpt-image-2` 拉大 `size` 并非真 4K，清晰度低、画质差；且 PSD 模板固定 1024×1536。生图与 PS 合成逻辑恢复 **v1.29 口径**（1024×1536）。勿再改 `OPENAI_IMAGE_SIZE` 除非同步重做 PSD 模板。

## v1.31

- v1.30 误把 `OPENAI_IMAGE_SIZE` 提到 2336×3504：PSD 模板画布固定 1024×1536，`placedLayerReplaceContents` 大图会裁切错位 → 已回滚生图尺寸；`apply_photoshop_template_batch` 合成前按 `PHOTOSHOP_TEMPLATE_INPUT_SIZE` 对齐尺寸。已用大图跑过的菜品可只重跑 PS 合成修复 `publish/final/`。

## v1.30

- 生图默认尺寸升为 **2336×3504**（2:3 在 gpt-image-2 官方约束内最大）；`V2/config.env` 的 `OPENAI_IMAGE_SIZE`，模型仍 `gpt-image-2` 自适应档（丝路 image2 Key 下 `-4k` 专用模型 503，用 size 拉分辨率更稳）。
- `resolve_openai_image_model` 透传 `gpt-image-2-1k/2k/4k`；仅无效快照名（如带日期后缀）映射回 `gpt-image-2`。

## v1.29

- 锚点补生后海报未进发布图：PS 的 `only_kinds={detail,recipe,cover}` 排除了 poster，即使 `force_all` 也不合成海报；现 `force_photoshop_all` 时 `only_kinds=None` 四张全合成。
- 非发布图仍见 publish 副本：看图栏「非发布图」现过滤 `/publish/` 路径；锚点海报仍 copy 留根目录候选。
- 菜谱悬空筷：选图前 `filter_defective_publish_candidates` 剔除；全灭则 `select_publish_group_with_defect_regeneration` 重生成一轮。
- 多主材细节图：菜名解析多种主料（鳝段+嫩鱼），注入细节图模板豆包 system prompt + `xijietu.txt` 三宫格说明。

## v1.28

- 面板空白根因（v1.27）：`submitAnchorPosterRegenerate` 里 `].join("\n")` 写在 Python `HTML_PAGE` 三引号内，`\n` 变成真实换行 → 浏览器整段 script SyntaxError。内嵌 JS 换行须写 `\\n`。

## v1.27

- 锚点海报补生：看图栏「设为海报并补生」→ `history/{timestamp}/publish|final` 存档旧发布图 → 复制候选海报入 `publish/` → 补生 detail/recipe/cover + `force_photoshop_all` PS 至 `final/`。锚点须不在 `publish/` 与 `history/` 内；细节/菜谱/封面 count/quality 走 `apply_runtime_overrides` 与补生相同 env。

## v1.26

- 公众号封面裁剪须点裁剪弹窗内第二个 `cover-preview-card`（`edit-cover-type='3_4'` 下 nth(1)），不是「3:4（转发卡片）」文字标签；选中后再拖拽 (662,555)→Y=410。探测：`V2/weixin_cover_crop_click_test.py --try-select-forward`。

## v1.25

- 自定义生图历史乱：旧版 `.custom-tile` 1:1 方格在窄栏易叠成细条；改 `.custom-job-card` 列表，按 job 分组展示缩略图+截断 prompt。

## v1.24

- 发布计划新增 `published_dates` 数组（`publish_plan.json`），每日右侧复选框切换；已发布日期 `.plan-day.published` 绿色边框便于区分。

## v1.23

- 补生整任务失败根因：`run_supplement` 菜谱生图失败后继续跑封面，封面 `generate_cankao_prompt_by_template` 遇 Connection error 未捕获直接 raise；主流程 `run_v2_mode2` 有 try/except 补生没有。网关 500 unusual activity 为 silkroadai 限流，非代码回归，但需退避重试。

## v1.22

- 批量生菜 `NameError`：`find_recent_auto_dish_history_conflicts` 用了 `ingredient_appears_in_text` 但未从 `market_ingredient_library` 导入。面板生菜未传 `model_temperature`，`image_generator` 也不读 `MODEL_TEMPERATURE`，导致调 0.6 无效。

## v1.21

- 批量生菜仍失败根因：校验层叠（结构族+风味族+做法族+尾字+主材子串）导致 4 次重试全灭；豆包单次 30～60s，前端轮询断开触发 `ConnectionAbortedError` 误似崩溃。现精简校验、8 次换锚点、批量会话主材去重、轮询断连忽略。

## v1.20

- 批量生菜报「近作主材方向：豆制」根因：① `image_generator.ensure_runtime_config_loaded` 用根 `config.env` 覆盖 `AUTO_DISH_MEMORY_FILE=dish_idea_memory.jsonl`；② 近作去重用品类族+全文子串，描述里「豆豉/豆腐」也会命中。现不覆盖已设 env、按记忆 `main_ingredient` 只查菜名、重试换锚点。

## v1.19

- 自动造菜根因：旧版 `build_auto_dish_generation_system_prompt` 为空、`AUTO_DISH_*_FAMILIES` 空元组、参考菜整道继承 → 换皮家常菜。现用 `V2/cankao/zhushicai.txt` 菜市场食材库 + `market_ingredient_library.py` 随机锚点（菜式/主材/改刀/风味/载体），双附件（食材库+传统菜库），校验主材体现与近作族冲突。
- 配置：`AUTO_DISH_INGREDIENT_LIBRARY_FILE=V2/cankao/zhushicai.txt`。

## v1.18

- 发布报「未找到 publish/final」：不少菜只完成豆包选图入 `publish/`，未跑 PS 合成则无 `final` 子目录；现 `resolve_publish_final_dir` 优先 final、回退 publish 根目录按关键词找 01–03 图。

## v1.17

- 平台标题碗饭套话泛滥：豆包 prompt 与 `parse_v2_publish_platform_payload` 双重禁用「连炫/连吃N碗」「N碗饭」等；触发 `TEXT_REQUEST_RETRY_COUNT` 重写。

## v1.16

- 公众号封面裁剪图标默认 `visible=False`，须 hover `.share_cover` 后才显示 `a.js_modifyCover`；确认按钮限定 `div.weui-desktop-dialog:visible` 内绿色「确认」，避免误点页面其它按钮。

## v1.15

- 公众号登录模板按最大化视口截取；须在 `match_template_on_page` **之前**最大化，不能等到 pyautogui 上传阶段。`prepare_weixin_chrome_page` 在 resolve/登录循环/贴图页/上传统一调用；CDP 成功也须 `IsZoomed` 校验，否则 Win32 + Win+↑。

## v1.14

- 公众号上传坐标 (616,380) 等按**最大化 Chrome**标定；仅置顶不最大化时浮动窗口会使 pyautogui 点偏。`activate_chrome_window_for_page(maximize=True)` 默认 CDP `windowState=maximized`，失败再 Win32 `IsZoomed`/`ShowWindow(3)`。

## v1.13

- 公众号 pyautogui 上传/封面拖拽：`page.bring_to_front()` 只切标签，不激活 OS 窗口；须 `EnumWindows` + `SetForegroundWindow` 将 `Chrome_WidgetWin_1` 置顶，按页面标题/「微信公众平台」匹配窗口。

## v1.12

- 「面板已断开」误报：v1.11 用 `/api/state` 做 ping，发布时并发多、响应慢，2.5s 超时即标离线，但 `/api/publish_status` 仍正常故日志继续刷。改 `/api/ping` + 连续 3 次失败才离线 + 轮询成功即恢复。

## v1.11

- 发布计划点发布报 `Failed to fetch` 根因：8765 面板进程已停止（常因点了「终止程序」未重启），非菜品或公众号逻辑错误。修复侧重离线检测与明确提示；重启后 `墨式辣炖牛肋` 公众号发布可正常启动。
- 重启 bat 勿用 `CREATE_NO_WINDOW` 直接跑 `cmd /c bat`，改 `start /min` 新开窗口更稳。

## v1.10

- 顶栏「终止/重启」：`schedule_panel_shutdown` 清空任务队列、结束发布子进程、`server.shutdown()` + `os._exit`；重启走仓库根 `restart_v2_web_panel.bat`（与手动双击等价）。

## v1.09

- 发布登录弹层不弹根因：`weixin_mp` 等只 `print` 提示语后 `input()`，面板 `LOGIN_WAIT_PATTERNS` 匹配不到整行；须在 `input` 前 `print("登录完成后请输入 y 继续", flush=True)`。统一用 `tools/publish_login.wait_for_panel_login`；发布中 `fetchPublishStatus` 500ms 轮询。

## v1.08

- 平台标题计字：`count_publish_title_units` 非 ASCII（含表情）计 2；`normalize_publish_title` 全平台生效，超 40 自动截断。
- 公众号封面裁剪在 `save_as_draft` 后执行；坐标默认 (662,555)→Y=410，先点 3:4 区第二个 `cover-preview-card`（转发卡片）；逻辑在 `tools/weixin_mp_publish.py`。

## v1.07

- 发布计划只显示菜名无图：v1.03 为让首屏过滤生效把 `initPublishPlanUi` 提前到 `loadHistory` 前，渲染时 `historyItemCache` 无 `preview_image`；计划项还常不在菜品池首屏分页内。现 `hydratePublishPlanCache` + 调整加载顺序修复。

## v1.06

- Python 用户包已迁到 `D:\Python\Roaming\Python`，但 `C:\Users\Lenovo\AppData\Roaming\Python` 联接未建成（~76MB 残缺）。用全局 `C:\Python312\python.exe` 跑面板会导致发布脚本 `pip install --user` 失败（WinError 5）。
- 本项目依赖在 `.venv`（已验证 playwright/Pillow 等齐全）。必须用 `restart_v2_web_panel.bat` 或 `.venv\Scripts\python.exe V2/web_panel.py`；勿用裸 `python`。v1.06 起面板/发布自动 re-exec 到 venv。

## v1.05

- 选中态统一走 `selectedHistoryPath` + `selectionSource`（`pool`|`plan`）；发布仍用 `startPublish` 读 `selectedHistoryPath`，计划内菜品单击即可发布。
- 拖入/移出计划后 `reconcileSelectionSource()` 根据池可见性自动切换高亮所在栏。

## v1.04

- 重启后前端全空白根因：`web_panel.py` 内嵌 JS 写 `replace(/\\/g)` 时 Python 只保留一个 `\`，浏览器收到 `replace(/\/g` → **SyntaxError**，后续 `loadState`/`loadHistory` 均不执行。内嵌 JS 中匹配反斜杠须写 `\\\\`（输出 `\\`），或改用 `split(/[/\\\\]/)` 避免 `replace`。
- 排查法：curl 首页 → 抽出 `<script>` → `node --check` 可快速定位语法错误行号。

## v1.03

- 标签弹层被裁切根因：`.history-cover` 的 `overflow:hidden` 裁掉了绝对定位的 `.tag-popover`；封面图改包在 `.history-cover-media` 内裁剪。
- 发布计划「移动」：`collectPlanAssignedPaths()` + `applyHistorySearchFilter()` 隐藏已分配 path；`assignPlanSlot` 赋 path 时清除其它格同 path；计划 chip 双击走 `openPlanDishLightbox`，`lightboxSource=plan` 避免联动看图栏。
- 自定义生图打开目录：`dirnamePath(tile.path)` 打开 `images/<job_id>/` 而非 `custom_image_gen/images/` 根目录。
- `loadState` 须在 `loadHistory` 前初始化 `publish_plan`，否则首屏过滤不生效。

## v1.02

- 餐次标签存 `dish_meal_tags.json`（path→breakfast/lunch/dinner/late_night 数组）；发布计划存 `publish_plan.json`（日期→早/中/晚 path）。布局新增 `--col-plan` 默认 200px，`v2_panel_layout_v3` 记忆栏宽。
- 发布计划与菜品池用 HTML5 拖拽（`application/x-dish-path`）；拖回菜品池即清空对应计划格。

## v1.01

- 6/10 多道菜无图根因：丝路 `gpt-image-2` 单次 `n=2` 报 `tools[0].n`（400），`api_error` 不重试故海报直接失败；偶发 n=2 返 1 张时第二轮 n=1 能继续。`get_image_api_batch_n()` 丝路强制 n=1。
- 6/10 共 21 道菜：7 道 0 图、14 道日志含 tools[0].n；完整四组图仅 6 道。失败菜可用「指定+补生」重跑。

## v1.00

- 分栏拖不动根因：`@media(max-width:1700px)` 写死 grid 列宽 + `应用三栏宽度` 按视口钳制。现改全固定列宽、`layout-scroll` 横向滚动、四分隔条分别调 left/gallery/mid/publish 宽度；localStorage `v2_panel_layout_v2`。

## v0.99

- 面板列序：菜品池 | 看图/文字(1fr) | 造菜 | 发布 | 自定义生图。左栏默认 540px 三列；`historySearch` 前端按菜名过滤。
- 看图灯箱 `#galleryLightbox`：双击主图/缩略图打开；滚轮与左右箭头切图并同步主画廊索引。
- 造菜参数 UI 固定默认 2 张 + high（覆盖 config 读入）；已移除 preset 模板按钮。

## v0.98

- 菜品池只显一列根因：左栏 320px + 卡片 min 168px。现默认双列紧凑卡、左栏 400px；头部下拉可切 1/2/3/自动列，选三列时自动拉宽左栏。

## v0.97

- 自定义生图：`custom_image_service.py` + 面板最右栏；multipart 提交 prompt/count/refs；后台线程调 `generate_images_by_prompt` / `generate_images_from_references`；历史 `custom_image_gen/history.json`；文件目录 `custom_image_gen/images/<job_id>/`。

## v0.96

- 气泡去重：`generate_poster_bubble_copy` 前 `sync_bubble_copy_history_file` 汇总全库 `*_气泡文案.txt` 到 `bubble_copy_history.txt`；提示词附带历史列表并要求不得重复；补生同一菜品时排除当前目录旧气泡以免误拦。

## v0.95

- 公网面板：`start_v2_public_panel.bat` 启动本地 8765 + `tools/cloudflared.exe` 快速隧道（trycloudflare.com），无密码；URL 见 `V2/public_tunnel_url.txt`。每次重启 URL 会变；链接泄露则他人可操作造菜/发布，注意保密。

## v0.94

- 丝路 `images.edit` 常 n=2 只回 1 张：旧逻辑整批重试 3 次仍 1/2 就失败。现 `execute_image_generation_with_retries` 累计已返图，下轮 `n=剩余张数` 补生（参考图不变），日志见 `已累计 x/y`、`补生剩余 z 张`。

## v0.93

- 丝路报 `model_not_found` for `gpt-image-2-2026-04-21`：根因是 V1 `image_generator.ensure_runtime_config_loaded()` 读根 `config.env` 覆盖 `OPENAI_IMAGE_MODEL`；V2 现每次生图前 `sync_v2_openai_image_settings()` 强制 `V2/config.env` + 丝路映射 `gpt-image-2`；根 `config.env` 默认模型也改回 `gpt-image-2`。

## v0.92

- 丝路参考图生图偶发 502/Connection error 或 n=2 只回 1 张：`execute_image_generation_with_retries` 最多 3 次；张数不足也重试；`images.edit` 每轮重开参考图文件；日志含 `error_type` 与 `第 x/3 次`。

## v0.91

- 生图报 `billing_hard_limit_reached` 但直连 silkroad 成功：根因是 `ensure_runtime_config_loaded` 用 `if key not in existing_keys` 跳过覆盖，Windows 用户/终端里若残留旧官方 `OPENAI_API_KEY` 或 `OPENAI_BASE_URL`，面板一直打官方额度。修复后 `config.env` + `.env` 始终覆盖；`build_openai_image_client` 会打印 `OpenAI 生图环境：base_url=…，key=sk-…`。改完后须 `restart_v2_web_panel.bat` 重启面板进程。

## v0.90

- OpenAI key 换 silkroadai：密钥仍放根目录 `.env` 的 `OPENAI_API_KEY`；网关地址配 `config.env` 与 `V2/config.env` 的 `OPENAI_BASE_URL`。V2 `build_openai_image_client` 原本已读该变量；V1 `build_image_client` 已补齐。

## v0.89

- 文字栏 UX：textarea 默认可编辑导致误触即 dirty、切换 prompt 文件体验差。现只读 + 编辑/保存/取消；切换分类或文件时 `tryLeaveTextEditorEdit` 确认放弃未保存内容。

## v0.88

- haibao 必备元素：旧写法「画面配{是否…}」易让豆包照抄成「画面配不配米饭」。现改为单变量「按菜品写清是否另配米饭及画面」，主食即本菜（面/饭/饺）与甜品汤品均不配饭。

## v0.87

- haibao 必备元素占位符内嵌 `{汤汁/肉汁，汤汁}` 时旧正则 `\{[^{}]+\}` 误拆成两个变量位，外层无法替换。现 `find_cankao_placeholder_spans` 按嵌套深度识别顶层占位符。
- 面板左上角仍显示 v0.83 根因：v0.84–v0.87 只改了 `项目规范.md` 未改 `PANEL_VERSION`。规则已改为每次版本递增必同步三处并重启面板。

## v0.86

- 面板重启入口统一为根目录 `restart_v2_web_panel.bat`（杀 8765 + 新窗口启动 + 打开浏览器），避免只在 Cursor 里改代码后旧进程未换代码。

## v0.85

- 菜品池重复根因：手动模式每次 `timestamp_菜名` 新建 output，旧 dish_pool/output 目录不删。现 `run_v2_mode2` 成功后 `dedupe_archive_duplicate_dish_folders` 按菜名移入 `dish_archive`；指定模式复用原目录不去重。

## v0.84

- 脆皮鲜椒乳鸽 PS 遗漏根因：旧逻辑见 Photoshop 已运行即失败。现 `close_running_local_photoshop` 在校验与每张合成前自动关 PS，补生只勾「PS 合成」即可补齐 final。
- Git 滞后：v0.67–v0.83 仅改本地文件未提交，远端仍停在 v0.66；2026-06-08 已一次性提交推送 `a6d286a` 至 `origin/2026-06-01-k9u1`（中间版本见 `版本记录.md`，非逐条 commit）。

## v0.83

- 任务条无反应根因：`.hidden` 无全局 CSS，跳转/展开按钮失效时仍可见；自动模式 `output_dir` 空导致跳转失败且提示在底部不易见。日志行「输出目录：」现回填 `live_output_dir`。

## v0.82

- 前端无反应/日志空白：v0.79 任务条在 `run_status` 内嵌套 `build_task_queue_snapshot` 双重 `Lock` 死锁；任务 worker 写日志也拿不到锁。改 `RLock` 后恢复。

## v0.81

- 进食真实性不靠逐项禁令：豆包与 Cursor 同类，一句统一原则即可——「海报夹起、细节送嘴的主菜，均须是人类吃该菜时通常会食用的部分」；v0.80 蟹壳正则校验已撤。

## v0.80

- 口味蟹夹壳根因：豆包填 `xijietu` 时把进食动作写成「梭子蟹蟹壳送嘴」，旧 system 无吃法约束且模板示例含「脆壳」易误导甲壳类。

## v0.79

- 任务可见性：`/api/run_status` 返回 `task_queue`（running + queued）；前端 `taskBar` 与菜品池角标解耦「查看上下文」与「后台任务」，避免切菜后忘记谁在跑。

## v0.78

- 气泡提示词：用户要求像对人说话一样简短交代即可，不要示例口径和机械禁令；`build_bubble_copy_prompt` 一两句话说明角色与意图，字数校验保留（气泡排版硬约束）。

## v0.77

- 气泡拟人根因：旧提示「用我第一人称 + 口感形容词」易产出「我鲜得…」（菜在说话）。现说话人定为阿叶厨师；`generate_poster_bubble_copy` 附带 juese 角色图。

## v0.76

- 菜品池交互：收藏/移出只做本地 DOM 更新；`history_revision` 不再含 favorites mtime，避免轮询触发重排；造菜任务完成（`wasRunning`）或手动「刷新菜品池」才全量 reload。
- 已生图优先：`folder_has_generated_image` 检查目录与 publish 下图片后按 mtime 倒序。

## v0.75

- Cursor 二次卡顿根因：① Python 语言服务全项目分析（右下角「16 files to analyze」）；② 面板在 Cursor 终端里跑时 `/api/state` 等 GET 每秒刷日志；③ 资源管理器展开 `dish_pool` 渲染大量图片目录。
- 已处理：面板静默轮询 GET；`pyrightconfig.json` + `python.analysis.diagnosticMode=openFilesOnly`；`.cursorignore`/watcherExclude 继续屏蔽 chrome 配置与 dish_pool。

## v0.74

- 菜品池删除改为归档：移入 `V2/dish_archive/dish_pool/...` 或 `dish_archive/output/...`，不 `rmtree`；收藏写 `dish_favorites.json`，排序默认收藏优先。

## v0.71

- 生菜附件：`generate_auto_dish_idea` 将 `chuantongcaipu.txt` 按 `AUTO_DISH_CUISINE_MODE` 截取对应分区后，作为第二条 user 消息（`【附件：chuantongcaipu.txt】`）一并发给豆包。

## v0.70

- 生菜模型：`doubao-seed-2-0-pro-260215`（Seed 2.0 Pro 旗舰）；mini 仅保留在 `DOUBAO_REVIEW_MODEL` 供图片评审降本。
- 生菜提示词：自然语言直发豆包，格式只约束「第一行菜名、第二行描述、不要空行」；近 10 条历史菜名仍注入避免重复。

## v0.68

- 细节图人物不对根因：曾为解决错菜只传海报参考；现 `build_detail_reference_paths` 固定 `[海报, juese.png]`，`generate_images_from_references` 多图带文件名并追加【参考图说明】。
- 运行参数查询：菜品目录 `_run_log.txt` + `_run_meta.json` 的 `image_generation_settings`；控制台会打印 `细节图生图参考图：海报=...，角色=...` 与 `参考图生图输入：...juese.png...`。

## v0.67

- PS 误合成根因：`apply_photoshop_template_batch_to_dir(publish_dir)` 会处理 publish 下全部图片；现改为按入选源图列表 + final 时间戳跳过已合成项。

## v0.66

- 指定造菜失效根因：`task_item` 把 `target` 模式清空为 `""`，且未传 `target_output_dir`。
- 看图栏消失根因：2s 轮询 `renderResult(LAST_RESULT)` 在「发布图」筛选下 `publish_final_images` 为空导致画廊被清空。

## v0.65

- 历史预览不同步原因：点击菜品池只填了菜名目录，未读 `publish/*筛选结果.json` 与 `final`；现用 `read_dish_output_summary` 统一回填主图/封面/选图/PS/流程状态。

## v0.64

- 指定造菜：`run_v2_mode2(mode="target", target_output_dir=...)` 复用目录内 `*_造菜信息.txt`，不调用 `build_run_output_dir`。
- 菜品池批量删除：`POST /api/history_batch_delete`；列表 `grid auto-fill minmax(168px,1fr)`。

## v0.63

- 面板发布：`POST /api/publish_start` 按所选平台顺序执行 V2 发布测试脚本；`input()` 登录等待通过 `/api/publish_login_confirm` 写 stdin 继续。
- 菜品池自动刷新：后端 `history_revision`（目录数+最新 mtime），前端 2s 轮询，运行中也会更新。
- 看图筛选：`publish_images` 仅 `publish/final`；`images` 为目录内全部图片（递归）。

## v0.62

- 视频号话题丢失：快手同款 `pyautogui` 粘贴易失焦/剪贴板未就绪，日志显示已粘 10 个但页面只剩末尾几个；改为 Playwright `Ctrl+V`、整行优先、缺项补粘并校验。
- 终端密密麻麻红字：多为把脚本日志误粘贴进 PowerShell，非脚本报错；以 exit code 与 `07_描述与话题输入后.png` 为准。

## v0.61

- 视频号描述截断原因：正文只校验前 12 字 + 话题粘贴时 click 把光标回到段首；已改为全文校验与 Ctrl+End。

## v0.60

- 视频号：主界面点「图文」会直达「发表动态」编辑页，无「发表图文」按钮；脚本在点击图文后检测编辑页并跳过该按钮。

## v0.59

- 视频号标题：上传后 DOM 变化，须 `locator.fill` 或剪贴板写入 `input`；勿仅用 `page.keyboard.insert_text`。
- 流程截图目录：`tools/weixin_channels_publish_steps/`。

## v0.58

- 微信视频号：锁定 `channels.weixin.qq.com`；图1 登录检测；模板点击「内容管理→图文→发表图文」；上传 01/02/03；`_图文标题.txt` + `_微信视频号和公众号图文描述.txt`；话题剪贴板粘贴。
- 测试：`python V2/weixin_channels_publish_v2_test.py`（勿动鼠标）。

## v0.57

- 快手话题粘贴已接入 `run_kuaishou_publish` 全流程；测试脚本 `kuaishou_publish_v2_test.py` 与正式逻辑一致。

## v0.56

- 快手话题：逐个复制 `#话题` 到剪贴板，作品描述框 Ctrl+V；后续话题先空格再粘贴（用户实测可行）。
- 测试：`python V2/kuaishou_topic_enter_test.py`（默认两个话题）。

## v0.55

- 快手话题确认改为系统主键盘 Enter（`pyautogui`）；输入改为 `press_sequentially`；勿用 `NumpadEnter`（会空行、不变蓝）。
- 临时测试：`python V2/kuaishou_topic_enter_test.py`（默认 `main_enter`）。

## v0.54

- 快手上传修复：必须点击「上传图片」真实上传；上传后关闭 Win 对话框并进入编辑页；锁定 publish 页而非 manage 页。

## v0.53

- 快手作品描述顺序：`_图文标题.txt` 标题 + 空行 + `_快手图文描述.txt` 第 1 行正文 + 空行 + 第 2 行话题。

## v0.52

- 快手发布修复：上传不再遗留 Win 对话框；标题用 `_图文标题.txt` 回退；话题逐个回车确认（2 秒间隔）。
- 发布脚本统一 `runtime_deps` 自动装依赖到当前 Python（含 .venv）。

## v0.51

- 新增快手发布脚本：发布作品→上传图文→三张图+封面；作品描述填标题与 4 话题（回车确认）；作者声明「个人观点」；所在地区选成都市。

## v0.50

- 小红书发布优化：DOM 上传三张图+截图校验；正文/话题分开，话题等 1s 回车确认；地点点下拉首项。

## v0.49

- 新增 V2 小红书发布脚本：CDP 锁定 creator.xiaohongshu.com；图匹配「发布笔记」；坐标发布图文；一次上传 01/02/03；填小红书标题与图文描述（含话题）；原创声明与添加地点成都市。

## v0.48

- 用户要求：公众号话题不超过 10 个；V2 与 V1 豆包生成处统一改为 10 个微信/公众号话题。

## v0.47

- 用户要求：为 V2 创建抖音发布独立测试脚本，先复用 V1 旧发布流程；测试目录为 `V2/output/20260604_210303_葱香陈皮羊排`，图片必须使用 `publish/final`。
- 已实现：新增 `V2/douyin_publish_v2_test.py`，强制按海报图、细节图、菜谱图上传，封面图单独上传；已实际跑通并停在人工发布前。

## v0.46

- 用户要求：看图栏旁增加文字栏，点击菜品后分类展示相关文案；按钮悬停与选中状态要明显。
- 已实现：新增 /api/text_assets 递归读取 txt，并按造菜信息/平台文案/提示词/其他分组；前端增加右侧双标签与按钮蓝色 hover、绿色 active。

## v0.45

- 用户要求：去掉模式1；模式2 作为唯一流程；面板增加仅豆包生成造菜信息 txt（可选数量，目录 `0001_菜名`）。
- 已实现：删除模式1 入口与 UI；`idea_batch.run_idea_batch`；历史列表合并 `dish_pool` 子目录。

## v0.43

- 用户反馈：图文标题/描述太模板化；要求把菜名、做法、筛选海报一并发豆包，各平台用爆款钩子式任务（抖音 4 话题、小红书 10 等），去掉 V1 死板提示。
- 已实现：`generate_v2_publish_copy_assets` 多模态 + `V2_PUBLISH_PLATFORM_TASKS`；`persist_v2_publish_copy_assets` 在海报/主图入 publish 后调用；模式2 `mode2_flow`、模式1 `main` 不再流程开头生成平台文案。

## v0.42

- 气泡文案过长问题：收紧 `generate_poster_bubble_copy` 为 5–12 字第一人称口感句，不合格自动重试。

## v0.41

- 约定：改完 `web_panel.py` 后 AI 应自动重启 8765 面板进程；用户浏览器需 Ctrl+F5。

## v0.40

- 用户要求：V2 两种模式自动造菜增加菜系下拉，默认中华料理。
- 已实现：`#cuisineMode` 下拉 + `apply_runtime_overrides` 写 `AUTO_DISH_CUISINE_MODE`；恢复 V1 菜系配置表。

## v0.39

- 模式2 细节/菜谱/封面多图时豆包选优入 publish；全部选完后 PS 输出到 publish/final，原图保留在 publish 根目录。

## v0.38

- 用户要求：造菜后输出目录要有造菜信息 txt；气泡文案改第一人称；并输出与 V1 一致的多平台标题/话题/描述文件。
- 已实现（v0.38，v0.43 已调整时序）：造菜信息立刻落盘；平台文案在 v0.43 改为选图后多模态生成，失败写 `*_平台文案生成失败原因.txt` 不中断主流程。

## v0.37

- 用户要求：新增模式2（海报/细节/菜谱/封面四组图），原流程为模式1；前端默认模式2，两组按钮均支持自动造菜与手动点名。
- 已实现：`mode2_flow.run_v2_mode2` 串联 haibao 模板生图、抖音海报单句筛选、气泡文案、xijietu 多模态模板、caipu/fengmian 参考海报生图；提示词均落盘为 `*_文生图prompt.txt`。
- 前端：`web_panel` 增加模式1/2 切换与四组画质/数量；`config.env` 增加 `V2_WORKFLOW_MODE` 与 `MODE2_*` 参数。
- 校验：仅做 `py_compile` 与占位符解析本地测试，未调用生图 API。

## v0.36

- 用户反馈：历史记录点击后，信息卡里的“菜系”“参考菜”仍显示“未记录”，与实际自动造菜结果不一致。
- 根因：历史列表接口仅按目录名和图片聚合，没有持久化读取每轮任务的结构化元数据。
- 已修复：任务完成时写入 `_run_meta.json`，历史接口读取并返回 `region_label/reference_dish`，前端历史预览可正确显示。
- 兼容兜底：同会话下若元数据文件暂未落盘，接口会从 `LAST_RESULT` 内存结果补齐字段，减少短时空窗。

## v0.35

- 用户反馈：前端运行后出现目录不完整（仅主图候选，无封面/无 publish 完整结果）。
- 根因补充：评分链路存在“无首选返回”或异常时的中断分支，导致主图首选为空，后续封面生成条件不成立。
- 已修复：主图评分与封面评分均增加 `fallback_direct`（评分失败或无结果时，自动选首张并入 publish）。
- 结果：即使评分侧不稳定，整体流程仍可产出“主图+封面”完整结果，前端不再出现半程完成观感。

## v0.34

- 用户反馈：前端一次运行后目录只有一张主图，结果看起来“输出不完整”。
- 定位：存在主图已入选但封面链路中断/失败的场景，导致 `publish` 只落一张图。
- 已修复：主流程新增“封面缺失自动补偿”机制（检测到主图存在且封面缺失时，自动再跑一次封面生成+选优）。
- 可观测性增强：补偿失败会落盘 `封面补偿失败原因.txt`，避免无报错但缺图的黑盒状态。

## v0.33

- 用户反馈：V2 前端需要手动按 `F5` 才能看到最新历史与运行结果，体验割裂。
- 已实现：前端新增静默自动轮询（5 秒）机制，空闲状态下自动同步运行状态与历史变化。
- 稳定性处理：仅在历史快照变化时重载历史卡片，降低不必要刷新与界面闪动。
- 交互补充：页面重新可见时立即同步一次，避免后台运行完成后用户返回页面仍看到旧数据。

## v0.32

- 用户反馈：`红油蚕豆藕丝` 提示词把米饭与主菜描述成混合搭配，偏离“菜是菜、饭是饭”的真实进食场景。
- 根因：豆包变量改写阶段未对“米饭变量/互动变量”给出足够强的结构化语义约束，模型会把“铺垫在米饭上”误扩展为混合菜饭。
- 已修复：在豆包系统提示中新增强约束（米饭独立成碗、仅少量主菜点缀、主盘仍保留主菜主体、互动为入口前动作）。
- 兜底机制：程序层对第3/4变量强制标准化重写，保证即使豆包跑偏也输出正确的“分离式菜饭互动”文案。

## v0.31

- 用户反馈：`publish` 中豆包最终首选图存在，但前端看图栏未显示。
- 根因：历史图聚合逻辑仅读 `*.png`，而 Photoshop 覆盖后主图/封面常转为 `*.jpg`，导致入选图被漏读。
- 已修复：统一按 `png/jpg/jpeg` 聚合输出目录与 `publish` 子目录图片，前端历史预览与详情图同步展示。

## v0.30

- 用户补充规则：主图或封面若只生成 1 张，不要调用豆包评分，直接入 `publish`。
- 用户新增功能：沿用 V1 的 Photoshop 合成流程，对 `publish` 中筛出的主图与封面执行模板合成并覆盖。
- 已实现：数量=1时改为直通入选；当主图与封面首选都存在时，自动触发 `apply_photoshop_template_batch_to_dir(publish)` 批处理覆盖。
- 已实现：前端新增“选图方式 / PS合成”状态字段，避免用户误判当前是否走了豆包评分或 PS 后处理。

## v0.29

- 用户需求：新增两项大功能——主图评分选优入 `publish`，以及“首选图 -> 封面生成 -> 封面评分选优入 publish”，并且前端要可配置、可见结果。
- 已实现：主图生成后自动触发豆包多维评审，按评分选出主图首选并移动到 `publish`，同时保留评审报告。
- 已实现：封面提示词改为读取 `cover_promtp_cankao.txt` 并仅替换菜名变量；使用主图首选图作为参考输入调用 `gpt-image-2` 生成封面。
- 已实现：封面生成后再次自动评审并选出封面首选到 `publish`；前端新增封面数量、首选结果展示和打开 `publish` 操作。

## v0.28

- 用户要求：自动模式下隐藏/禁用补充说明，避免误导；并要求“重新生成”必须是严格复用原提示词与原参数语义（仅允许调整生图质量与数量）。
- 已实现：自动模式会隐藏并禁用补充说明卡片，仅手动点名模式显示与生效。
- 已实现：重新生成改为独立“重新生图”任务，从当前选中输出目录读取原始 `*_豆包提示词.txt`，只按当前画质与数量再次调用生图，不重新造菜、不重写豆包提示词。

## v0.27

- 用户反馈：看图栏信息卡中两个字段都显示“历史记录”，语义不明确，无法区分“参考菜”和“菜系”。
- 根因定位：历史卡片点击事件里把 `参考菜/菜系` 两个值硬编码为同一文本“历史记录”。
- 已修复：历史预览改为按字段显示缺省文案“未记录参考菜”“未记录菜系”，不再出现重复且误导性的同值展示。

## v0.26

- 用户需求：去掉“常用菜快速生成”模块，并澄清“自动造菜 + 补充说明”的真实生效逻辑。
- 已落地：删除快捷菜模块及对应点击即运行逻辑，避免误触后直接发起任务。
- 逻辑确认：补充说明仅在手动点名时写入 `dish_name.txt` 并传入豆包；自动造菜模式不会读取该输入作为约束。

## v0.25

- 用户需求：本地电脑开机后自动启动 V2 服务，进浏览器即可直接访问，不想每次手动运行脚本。
- 落地方案：优先尝试计划任务自启，但当前环境被系统权限拒绝；改为“用户启动文件夹自启”方案，无需管理员权限。
- 已完成：新增 `start_web_panel.bat`（含重复进程检测 + 隐藏启动）并写入启动项 `V2_WebPanel_AutoStart.cmd`，保证登录后自动拉起 `web_panel.py`。

## v0.24

- 用户反馈：看图栏顶部切图区出现“半截显示”，缩略图与按钮区不完整。
- 已修复：将切图控件放入独立容器并固定最小高度，改为上按钮下缩略图的双层布局，避免被后续区域挤压。

## v0.23

- 用户反馈：切图控件在看图栏下方，必须滚轮下移才能操作，不够顺手。
- 已修复：把上一张/下一张与缩略图选择整体上移到看图栏顶部，进入看图区即可直接切图。

## v0.22

- 用户反馈：历史输出卡片里按钮和菜名存在重叠，影响阅读与点击。
- 已修复：重构历史卡片为分区布局，操作按钮独立放到底部操作栏，不再遮挡缩略图或文字信息。

## v0.21

- 用户反馈：看图栏存在遮挡（运行态覆盖主图）且图片默认适配不佳。
- 已修复：运行中若已有图片仅显示角标进度，不再盖住整张图；主图适配策略改为容器内完整自适应显示。

## v0.20

- 用户反馈：结果信息卡覆盖在图片上，影响看图体验。
- 已修复：信息卡移出图片区域，改到图片下方展示；样式改为紧凑信息条，确保图片完整可见。

## v0.19

- 用户反馈：当前改版后仍不够顺手，明确要求三栏结构（左历史、中控制、右看图）且允许自行拖拽调整宽度并记忆。
- 已落地：新增三栏布局与双分隔条拖拽，默认右侧看图栏更宽；历史输出迁移到左栏并改为目录风格（缩略图+菜名）。
- 已落地：分栏宽度写入本地存储，刷新或重开页面后自动恢复用户上次调整结果。

## v0.18

- 用户需求：用图2作为网页图标，解决标签页无 favicon 的问题。
- 已落地：复制图2为 `V2/favicon.png`，前端 `head` 增加 icon 链接，服务端补齐 favicon 路由用于浏览器自动请求。

## v0.17

- 用户明确偏好深色界面，并给出完整交互建议：3:7 布局、抽屉日志、卡片历史、运行按钮进度、快捷生成与导航按钮分组。
- 已落地：前端重排为左侧固定控制 + 右侧结果历史；颜色、背景、层级切换为深色模式，顶部操作按钮与状态信息集中展示。
- 已落地：历史区域升级为双列图片卡片并支持悬停操作，日志改成可折叠抽屉（清空/只看错误），整体操作流程改为“左调参、右看图、底部按需看日志”。

## v0.16

- 用户反馈关键问题：当上一轮（如“焖酿蹄花”）图片还未返回时，前端再发起新任务，上一轮像“石沉大海”，无法确认是否继续执行。
- 根因定位：原面板是“单任务即时执行 + 覆盖式状态”，新请求要么被拒绝、要么覆盖当前可见状态，用户感知上像上一轮被吞掉。
- 已修复：后端改为任务队列顺序执行，新任务统一入队；运行态可继续“加入队列”，状态与日志显示队列和任务编号，确保每轮都有执行与可追踪反馈。

## v0.15

- 用户新增要求：双击图片要能新标签页打开；结果区红框信息不能空白；默认无图状态要改为美观提示；整体视觉参考 OpenAI 页面风格。
- 已落地：主图与缩略图都支持双击新标签打开；历史记录返回菜名并在右侧结果区同步展示，空值统一为中文可读占位。
- 已落地：无图/加载失败引入专用占位态（含图标与说明），避免浏览器破图观感；页面配色与顶部结构改为深色头部 + 浅色内容区的简洁布局。

## v0.14

- 用户痛点：当前面板“非人性化、难操作”，尤其图片一多后难以分辨；并要求前端去掉英文展示、增加版本号用于识别。
- 已落地：重做页面色调与信息层级，采用主图固定区 + 缩略图网格 + 历史卡片预览图，降低多图浏览认知负担。
- 已落地：页面文案改为中文优先，不再向用户暴露英文组件信息；顶部新增 `版本` 标签，当前为 `v0.14`。

## v0.13

- 用户反馈：第一版网页面板“从专业网页角度体验不佳”，希望从多维度自评并优化。
- 已完成：围绕信息层级、可读性、交互反馈、容错和移动端适配做第二版 UX 升级，保持后端调用链不变。
- 当前面板重点提升：运行状态更可感知（秒级计时）、手动模式校验更明确、历史预览可定位、操作路径更短。

## v0.12

- 用户需求：先看一版“UI 友好、方便人类操作”的 V2 前端网页面板。
- 已实现：`V2/web_panel.py` 内置单文件前端页面 + 本地 HTTP API，直接调用 `run_v2_first_feature()`，无需改动主流程入口。
- 首版能力：模式切换、手动菜名输入、温度/质量/数量调参、一键运行、结果与图片预览、最近输出历史。

## v0.11

- 用户要求：以后无需手动点 Review/Commit，V2 流程由 AI 自动同步仓库。
- 已落地：规范中新增“默认自动执行 `git status -> commit -> push`”的硬规则，并保留“用户明确禁止或远端阻塞”例外。

## v0.10

- 用户问题确认：V2 自动造菜缺少历史创意记忆与参考菜追溯，导致近期结果口味方向重复（如连续“蒜香”）。
- 已修复：V2 自动造菜直接复用 V1 核心函数，启用 V1 同款反重复策略与历史禁用约束。
- 已落地：新增 `V2/dish_idea_memory.jsonl`，并在日志中打印参考传统菜和记忆文件路径，方便排查“由哪道传统菜衍生而来”。

## v0.09

- 新要求确认：只有含 `{变量}` 的位置可以改写；未含变量的模板行必须保持 100% 原文。
- 已落地：豆包不再输出整段 prompt，而是仅输出变量替换数组；程序负责把替换值填回原模板。
- 结果：模板固定文本（尤其 `文案` 行）不会被模型改写，避免调试过的 prompt 被意外污染。

## v0.08

- 补充修正：v0.07 的重排虽然生效，但在少数单段输出里会把“真实拍摄”误判为“拍摄分类”起点，导致分段错位。
- 已修复：分类关键词匹配增加中文标点/空白边界约束，仅在更像“新字段起始”的位置触发。
- 同步增强：豆包提示词生成阶段新增“11 分类标签 + 空段”硬约束，优先从源头保障可读结构。

## v0.07

- 用户诉求：提示词文件需要像 `cankao.txt` 一样按分类分段，便于人类阅读和后续优化。
- 已实现：新增 `format_prompt_by_template_sections()`，先按模板抽取分类标签，再从模型输出中回填对应内容，最终按“分类 + 空行”格式落盘。
- 兼容性：支持模型返回单行长句、半结构化文本和模板化文本三类输出形态。

## v0.06

- 用户困惑点：`AUTO_GENERATE_DISH_IDEA=1` 时怀疑未自动造菜，以及“已有图片但终端出现失败信息”。
- 诊断结论：`20260601_031028_main_32364.log` 已确认该轮实际是自动造菜并成功生图；此前失败信息来自旧目录旧轮次，且 V2 存在多段重试分支容易造成感知混乱。
- 已修复：输出日志增加“开关值/命令行模式/最终模式”；生图流程改为更接近 V1 的单路径逻辑，仅保留超时重试。

## v0.05

- 新增根因：`V2/config.env` 中 `OPENAI_BASE_URL=` 空值会把环境变量设置为空字符串，OpenAI SDK 读取后会报 `UnsupportedProtocol`（上层表现为 `Connection error`）。
- 已修复：V2 生图客户端在空值场景强制使用官方默认 `https://api.openai.com/v1`。

## v0.04

- 进一步定位：同机直测 `OpenAI(api_key, timeout)` + `gpt-image-2` 能成功返回图片，排除 key 错误与官方网络不通。
- 根因收敛：V2 先前为生图客户端注入自定义 `httpx.Client`，与 V1 行为不一致，导致在当前环境下偶发/持续 `Connection error`。
- 已修复：V2 生图客户端改回 V1 同款初始化，仅保留 `api_key/timeout`（和可选 `base_url`）。

## v0.03

- 排查结果：`OPENAI_API_KEY` 读取变量名与 V1 一致，均为 `OPENAI_API_KEY`，并来自根目录 `.env`。
- 关键差异：V2 先前显式 `HTTP_TRUST_ENV=0`，导致不继承系统网络环境；在当前机器上该模式访问官方 OpenAI 会超时。
- 已修复：默认改为 `HTTP_TRUST_ENV=1` 并同步配置文件，现与 V1 的网络行为一致。

## v0.02

- 问题复盘：当豆包改写失败走本地降级模板时，旧正则仅匹配 `{变量}` 与 `{变量：...}`，未覆盖 `{变量，...}`，导致个别行残留占位符。
- 已修复：统一占位符规则为 `TEMPLATE_PLACEHOLDER_PATTERN`，降级替换和结果校验共用同一规则，避免再次漏替换。
- 结果：`cankao.txt` 中含中文逗号注释的变量位也能被按最新菜名语义替换。

## v0.01

- 目标：把 V2 做成与根目录主流程并行的独立版本，先实现首个功能闭环。
- 已实现：`AUTO_GENERATE_DISH_IDEA` 控制自动造菜或手动菜名读取。
- 已实现：读取 `cankao.txt` 后由豆包补全 `{变量}` 并生成最终提示词 txt。
- 已实现：把最终提示词直接投喂 `gpt-image-2` 生图，输出到 `V2/output/时间戳_菜名`。
- 默认配置：`OPENAI_IMAGE_QUALITY=low`，`OPENAI_IMAGE_COUNT=1`。

